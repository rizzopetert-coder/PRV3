# PRV3 Session Handoff — MOB v4.256

Direct extract/reformatting of this session's Section 16 entries in `tools/_mob.txt` (2026-08-27/28, five entries: the vercel.json outage, Aptitude addenda shipped, the Q27 gap elevated + retry edge case disposed, the Q27 fix shipped, and the SCD-WCS tooling consolidation). Not independently authored — if this ever appears to contradict Section 16, Section 16 is authoritative.

**Count note:** this file covers five Section 16 narrative entries, not nine — two other threads worked this session (the `built_to_fail` live-reachability correction, the `the_uninitiated` 17-vs-18/175 baseline resolution) were logged as direct corrections to Section 13a and `prompts/scd-wcs-remediation-tracker.md` respectively, not as their own Section 16 rows. Both are covered below in full regardless, since they're real, closed threads from this session.

## What this session covered

**CRITICAL: vercel.json missing narrative-prompt/narrative-process routes — a live Production outage, found and fixed.** Every live Path 1 session that reached the narrative-modulation trigger point hit a deterministic HTTP 500 since Narrative Modulation (Phase 3) deployed last session (commit `2d26718`/`9c57553`) — confirmed via `vercel logs` against the live deployment, not guessed: real server error `Narrative-prompt invocation failed: 404`. Root cause: `api/engine.py` defines real FastAPI routes at `/api/narrative-prompt` and `/api/narrative-process`, but `vercel.json`'s legacy routes array (required for this project's dual-runtime `@vercel/python` + `@vercel/next` config) was never updated to include them. Since `narrativeShouldFireNow` fires unconditionally at minimum on every session's last question, this was not an edge case — no real visitor session could complete Path 1 from that deploy until this session's fix. Root cause of the gap itself: that shipment's own verification was local/mocked only, never exercised the live Vercel routing layer — discovered only because this session ran a live production round trip for an unrelated change (Q35-Q39 wiring, below) and hit the bug directly. **Fix, commit `bc773a3`:** two route entries added to `vercel.json`, routing-config parity, no Gemini gate (Pete's explicit call). Verified via a real live production round trip: zero transient 500s across a full 50-answer session.

**Aptitude addenda (Q35-Q39) wired and live-verified — Path 1 fully live-verified end to end for the first time.** Q35-Q39 (authored Session 14, per `engine/data/questions.py`'s own comment — Pete's original reference named Session 13, a discrepancy noted but not chased further) wired into `PHASE_1_QUESTION_SEQUENCE` as plain unconditional core questions between Q34 and Q40 — no changes needed to `answer/route.ts` or the Python engine, since they carry no `severity_trigger` or conditional-splice metadata. Closes a pre-existing live/calibration mismatch: `calibration_runner.py`'s 175-profile suite already answered Q35-Q39 unconditionally for every profile, so the 166/175 baseline already reflected their real signal — live sessions had simply never generated it. Commit `f6f9bbd`. Live-verified in the same production round trip that confirmed the vercel.json fix: Q35-Q39 fired in exact expected order, contributing real dimensional signal to a completed result. **Path 1 is now, for the first time, fully live-verified end to end in one single pass:** checkpoints, a severity follow-on, the narrative prompt, and the Aptitude addenda all firing correctly together in one real production session.

**Narrative Modulation's Q27 timing gap ELEVATED; the retry-double-accumulation edge case CONFIRMED PARKED — two investigations, same thread, presented together because the contrast is the finding.** (1) The Q27 early-trigger timing gap (already documented as a residual limitation in a prior session) was elevated from "documented residual, no action" to "confirmed real, warrants a fix pass, Gemini architecture-gate required." Confirmed it fires under ordinary conditions (the same entropy-threshold mechanism already observed firing readily for Q11/Q19). A realistic test — 27 questions targeting `built_to_fail`, narrative fired early, then 20 more questions targeting a different state — cleanly flipped the reported primary state: the frozen snapshot said `the_unformed_leader` (0.858779), the true final vector said `built_to_fail` (0.938683). Confirmed blast radius broader than a prior session's precedent bug: the frozen snapshot directly determined the reported primary state, the full output package, per-state severity attribution, and the synthesis headline — all principal-facing, not diagnostic-only. (2) The retry-double-accumulation edge case (flagged in a prior session's completion-path fix) was confirmed to genuinely stay parked, now with real evidence: no organic trigger path exists in the shipped frontend (the loading-state UI renders no submit button during an in-flight request; the error-state UI offers "Start over," not retry). When artificially triggered, the effect is bounded and smaller than the Q27 gap.

**Narrative Modulation's Q27 timing gap SHIPPED — Pure Stateful Modulation with Completion Re-ranking.** `process_narrative_response()` no longer freezes a Q27-time ranking snapshot — it applies narrative's modulation to `accumulated_vector` in place and defers all ranking/ceiling computation to true completion, against the fully accumulated vector. `run_accumulated_engine()` reuses `enforce_state_probability_ceiling()` completely unchanged — no new mechanism. Data contract retired: `pre_narrative_rankings`/`post_narrative_rankings` collapsed to a single `pre_narrative_vector` field, threaded through 5 files plus `api/engine.py` (an unnamed call site caught mid-build that would otherwise have broken silently). Gemini architecture-reviewed; all 6 of Gemini's technical claims independently verified against live source before any code was written. Two precision notes from that verification pass are written directly into the shipped docstrings. Verification: tsc clean, vitest 45/45, all 11 Python engine test scripts pass, calibration unchanged at 166/175, standard-trigger parity confirmed both logically and empirically (zero behavioral drift). **Live production round-trip:** the new code's output was predicted locally in advance — `built_to_fail`, score 0.94937 — against the old buggy code's own precomputed prediction — `the_unformed_leader`, 0.846245 — for the identical scenario. The live deployed result matched the new-code prediction exactly. Commit `991a820`.

**SCD-WCS tooling consolidation SHIPPED — two new permanent tools replace a 34-script untracked scratch corpus** (corrected count — an earlier inventory this session undercounted at 31). `tools/scdwcs_validator.py`: `evaluate_candidate(modified_state, new_vector, new_salience, mode) -> ValidationReport`, consolidating three previously fragmented blast-radius methodologies into one shared function (default `mode="top3"`, verified as the optimal default — coarse suite-level checking misses flips within `SCD_WCS_CLUSTER_WINDOW=0.35`, full score-diff produces false positives on floating-point epsilon noise). Committed baseline (`tools/data/scdwcs_baseline.json`) with integrity-header staleness protection, `--rebaseline`-only regeneration. `tools/scdwcs_candidate_search.py`: uniform linear sweep only — coarse-then-refine explicitly rejected after this session confirmed real non-monotonicity in the actual scoring function against live engine data (19.4% of profiles show a genuine score-direction reversal at real search grid density). Consumes `evaluate_candidate()` as its sole evaluation engine, never auto-selects a winner. `tools/taxonomy_census.py`: relocated and tracked (was untracked scratch). Gemini architecture-reviewed as a consolidation of proven patterns; all 5 technical claims independently verified before build. Provenance preserved before deletion: 2 comment citations in `engine/data/salience.py` rewritten to describe methodology directly. One real bug caught during build (salience field-merge, not replace) via cross-checking a real scratch script — which also corrected an imprecise memory from earlier in the session. Two exact cross-validations against real historical scratch-script results (byte-identical `min_headroom_gap`, test_id locations, flip counts). 33 of 34 scripts deleted, 1 relocated; `tools/_salience_pilot_search_*.py` (6 files, separate family) confirmed untouched. Commits `24686bd`, `2a7c02b`.

**Two additional closed threads, logged outside Section 16 (in Section 13a and the SCD-WCS tracker respectively), covered here for completeness:**

- **`built_to_fail`'s live-reachability count corrected** (Section 13a Decision Register). A prior-session row claimed only 1 of 5 of `built_to_fail`'s wired core questions were live-reachable. Direct read of `engine/data/questions.py`'s `QUESTION_LIBRARY` state_targets found it's actually wired to 7 core questions, 6 of which are now live-reachable (only Q03A remains structurally excluded, tied to the locked Q03B-only intake adapter, unrelated to this session). Corrected in place.
- **`the_uninitiated`'s 17-vs-18/175 false-rank-1 baseline discrepancy resolved** (`prompts/scd-wcs-remediation-tracker.md`). Flagged as unconfirmed in a prior session. Re-run against the current live pipeline using two independent rank-1 methods — zero disagreements, current baseline confirmed as 18/175. The tie-order-artifact theory was disproven, not just left unconfirmed. Real cause: two legitimate, already-shipped scoring changes (Candidate C, the five-candidate re-authoring) landed between the two historical measurements — a real, fully-explained ripple, not a bug. Closed as a non-issue.

## Status at close

Path 1 is now fully live-verified end to end for the first time in the project's history — every mechanism (checkpoints, severity follow-ons, narrative modulation, Aptitude addenda) confirmed firing together in one real production session, and the timing-gap bug in narrative modulation's own ranking logic is fixed and live-verified. A live Production outage (the vercel.json routing gap) that had been silently blocking every completing session since the prior deploy is fixed. The SCD-WCS taxonomy-remediation program now has real, tested, permanent tooling in place of a 34-script untracked scratch corpus — the re-authoring work itself has not started, only the tools to do it safely now exist.

## Open — carried forward, not started or not resolved this session

1. **SCD-WCS re-authoring itself** — the tooling exists now (`tools/scdwcs_validator.py`, `tools/scdwcs_candidate_search.py`), but the actual state-by-state re-authoring work (the taxonomy-wide `dimensional_vector`/template project for `built_to_fail`, `invisible_performance_management`, `the_uninitiated`, `the_second_close` — confirmed not fixable via salience alone) has not started. Pete's call on whether/when to pursue it.
2. **`evaluate_candidate()`'s single-state scope boundary** — matches the reviewed contract, but multi-state combined candidates (the historical "Candidate C" pattern, two states changed together) are out of scope by design. Would need a future extension if that pattern recurs.
3. **Deployment Protection / no Preview environment / custom domain** — unchanged this session. The site is publicly reachable today at `prv-3.vercel.app`, unbranded, with no Preview environment as a safety net. Worth Pete deciding explicitly whether that combined posture is still acceptable.
4. **Real Transaction Path** — Phase 1 (Dropbox Sign e-signature engage flow) shipped in an earlier session (commit `5b9586f`) but carries manual steps not fully closed out; Phase 2 (the webhook) has not been started at all. Untouched this session — carried forward unverified, not re-checked.
5. Pre-existing carried-forward items unchanged this session: `session-store.test.ts`'s 6 stale sequence-length failures (last checked 2026-08-18); OSHA actual-average-penalty backfill (14 states, not urgent); `STATE_CAUSATION_OVERRIDES` per-state authoring (undated); ADA/FMLA/OSHA headcount gating (new logic, not started); `tools/diagnostic_fast_forward.py` rework-or-retire decision; the `organization_size` string|number type collapse, dated ~2026-09-04 (once ShareableOutput's 30-day KV TTL cycles past the relevant deployment).

## Closed this session

vercel.json outage (found and fixed, live-verified). Aptitude addenda (shipped, live-verified). Q27 narrative timing gap (investigated, elevated, Gemini-reviewed, shipped, live-verified). Retry-double-accumulation edge case (investigated, confirmed stays parked). SCD-WCS tooling consolidation (built, verified, shipped, 33 scratch scripts retired). `built_to_fail` live-reachability count (corrected). `the_uninitiated` 17-vs-18/175 discrepancy (resolved, closed non-issue).

## Parked — do not resurface unless Pete reopens

Attorney review of the Engagement Agreement / OneDigital covenant question (long-standing, parked indefinitely, Pete's explicit call). LinkedIn campaign go-live (blocked entirely on attorney review, not separately tracked). Real Transaction Path Phase 1's remaining manual steps and Phase 2 entirely (see Open, item 4 — parked/not started, not actively being pursued).

## Standing / dated items

- **Quarterly Step-Back — amended mid-session, 2026-08-28: cadence is now biweekly (every 2 weeks), not every 3 weeks.** Last step-back: August 23, 2026. Next due: **on or near September 6, 2026** — not yet due. (CLAUDE.md updated to reflect this, commit `903ff12`.)
- `organization_size` type collapse: dated ~2026-09-04, untouched, see Open item 5.

## Files to attach next session

- **Always:** `tools/_mob.txt` (current version, v4.256).
- **If resuming SCD-WCS re-authoring work:** `tools/scdwcs_validator.py` and `tools/scdwcs_candidate_search.py` are now the real entry points — the scratch scripts are gone. Also `prompts/scd-wcs-remediation-tracker.md` for the live tracking record, and `engine/data/states.py` / `engine/data/salience.py` for the actual vectors/salience being modified.
- **If revisiting narrative modulation:** `engine/main.py` (`process_narrative_response()`, `run_accumulated_engine()`), `engine/narrative.py` (`enforce_state_probability_ceiling()`), `tools/_verify_narrative_modulation_real_math.py`.
- **If touching API routes or the Vercel routing layer again:** `vercel.json`, and CLAUDE.md's new standing rule (API route changes require a live production round-trip check before being marked done).
- **If resuming the real transaction path (Phase 1 cleanup or Phase 2):** `web/app/api/engage/initiate/route.ts`, whatever Phase 1 completion-status doc exists from the shipping session (not verified this session — locate fresh).

## Mem0 diary status

Diary write completed this closeout via `tools/prv3_diary.py` (subprocess call, terminal Claude Code environment) — confirmed successful, topic `q35-39-narrative-q27-fix-scdwcs-consolidation`.
