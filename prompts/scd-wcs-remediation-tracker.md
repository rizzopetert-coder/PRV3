# SCD-WCS Remediation Tracker

Plain-language guide + working tracker for fixing which condition gets
named as "the" primary finding when a session's answers could point to
more than one state.

Pete has chosen the full clinical-completion path: individually correct
the vector/salience authoring across every affected state, not just
close the investigation with a summary. This file is the single place to
come back to across future sessions — read it first, update it as work
happens.

Full technical detail behind everything summarized here lives in
`prompts/scd-wcs-cluster-map-findings.md`. This file is the plain-language
index and the working tracker; that file is the record of how each
finding was actually verified.

---

## The four patterns found so far (plain language)

1. **Blends in** — this state looks mathematically identical to one or
   more others, so the engine picks between them almost at random. Fix:
   usually just needs a clearer, more specific "fingerprint."
2. **Overpowers everything** — this state wins way more often than it
   should, against many unrelated states, not just one twin. Fix: needs a
   real redesign of its fingerprint, not just a tweak.
3. **Steals from a whole other group** — this state doesn't have one
   obvious twin, it out-competes an entire cluster of other states at
   once. Fix: not yet understood, needs its own investigation.
4. **Unexplained** — doesn't fit any of the above yet, too little data
   to know why it happens.

## Legend for status column

- **NOT STARTED** — nobody's looked at this state yet.
- **DIAGNOSED** — we know what's wrong, haven't fixed it.
- **PILOT DONE** — a fix was tried and tested.
- **SHIPPED** — fix is live and verified.
- **DECLINED** — looked at it, decided on purpose not to fix it (real
  reason noted, not skipped by accident).

## Legend for fix-type flag column

Distinguishes two structurally different repairs that both currently get filed under Pattern 1 ("blends in"). Same-cluster differentiation is a salience-only fix within an already-correct vector family. Mis-clustering is a bigger question — the vector family itself may be wrong, which is not a same-cluster question and shouldn't be pursued with salience-only tools.

- **SAME-CLUSTER DIFF (confirmed)** — real descriptive_prose checked against the live dimensional_vector, dimension confirmed correct. This state just needs a distinct weight from its cluster-mates.
- **POSSIBLE MIS-CLUSTERING (open Q)** — real descriptive_prose checked, and the dominant dimension in the text does not match the state's current dominant vector field. May need a new `dimensional_vector` (a different dominant field), not just salience differentiation within the current cluster. Not scoped, no numbers proposed until its own pass.
- **Broad/cross-cluster (Track 2)** — the "overpowers everything" pattern. Needs real vector-level redesign, not a same-cluster question at all.
- **Cross-cluster asymmetry (undiagnosed)** — the "steals from a whole other group" pattern. Mechanism still unknown (see Open investigative questions below).
- **Unexplained anomaly** — doesn't fit any category yet, sample too small to theorize.
- **Already resolved** — SHIPPED or DECLINED; the fix-type question is moot.
- **TBD — not yet checked against real text** — a Pattern-1 "blends in" candidate that hasn't had this specific real-text dimension check done yet. Could turn out to be either same-cluster diff or mis-clustering once checked — `paper_shield` is the proof this isn't a safe assumption either way.

---

## Per-state tracker

| State | Pattern (1–4 above) | Fix-type flag | Cluster/rivals | Status | Pete's call: what actually makes this state different in real life | Dimension (optional) | Notes |
|---|---|---|---|---|---|---|---|
| `the_unformed_leader` | 1 — Blends in | Already resolved | Paired with `the_dormant_talent` (rank-7) | SHIPPED | | | Aptitude stayed dominant (capability gap — "without having been equipped for it"). Commit 043b8ad, 58a19a0-adjacent (second pass). |
| `the_dormant_talent` | 1 — Blends in | Already resolved | Paired with `the_unformed_leader` (rank-7) | SHIPPED | | | Landed magnitude keeps aptitude dominant, not the attitude-dominant read the real text argued for ("can name precisely... consistently doesn't act on it") — every candidate that made attitude dominant failed calibration. Narrative compromise, not a finished clinical differentiation. |
| `built_to_fail` | 2 — Overpowers everything | Broad/cross-cluster (Track 2) | Broad — 41/175 profiles (23.4%) as of Phase 8's re-measurement, down from the original 49/175 (28%) as other states' re-vectoring shifted some share elsewhere; spans all 4 dimensions, not one rival | DIAGNOSED (broad issue, root-caused) / SHIPPED (narrow tie-break only) | | | The tie specifically against `the_paper_tiger` is SHIPPED (commit 58a19a0). **CORRECTION, Program Phase 8:** the original "structurally unfixable via salience alone" claim was scoped to a narrower question than its wording implied — that pilot held `built_to_fail`'s own salience FIXED at 2.5 and tested only whether `the_paper_tiger`'s OWN salience could out-compete it, never built_to_fail's own weight. Phase 8 tested that directly: lowering `built_to_fail`'s own aptitude salience DOES dramatically reduce its false-rank-1 count (41 -> 0 by magnitude 0.1), with zero erosion of its 3 real own-profile wins down to 0.5. But this doesn't translate to real payoff — Phase 8's joint scoping found `invisible_performance_management` and `the_unsolved_problem` simply absorb the vacated dominance (whack-a-mole, confirmed across every tested single-state and combined-salience configuration, ceiling stuck at 1 of 19 masked states gaining anything). Genuinely confirmed unfixable via salience is the MASKED-STATE PAYOFF problem, not `built_to_fail`'s own footprint in isolation — a real fix needs taxonomy-wide vector/template re-authoring (Phase 8's census found 37 states share this exact template+skew shape, only 6 actively dominant), not scoped further. First named Track 2 candidate. **CORRECTION, Phase 9 (2026-08-21), this session — SUPERSEDES the "41/175" figure above:** re-verified with the same corrected methodology just used for `invisible_performance_management` (purpose-built full sweep + independent cross-check against the harness's own `build_confusion_matrix()` output, both methods hand-summed and agreeing exactly). **Live figure is 52/175 (29.7%), UP from the recorded 41 — a real drift, and in the opposite direction from `invisible_performance_management`'s.** Root cause, confirmed directly from the true-target breakdown: `built_to_fail` now wins ALL 3 of `paper_shield`'s own dedicated profiles (AUT-PS-01/02/03), ALL 3 of `the_unsolved_problem`'s (AUT-UP-01/02/03), and all 3 of `invisible_performance_management`'s (EXP-IPM-01/02/03) — 9 of the 11 new profiles accounting for most of the 41->52 rise. Mechanism: `built_to_fail` is itself Aptitude-dominant, same axis paper_shield/the_unsolved_problem re-clustered onto. The original 49->41 drop (recorded above) happened when those two states' re-clustering gave the Aptitude axis two new competitors to split share with; Phase 8's later mitigation (commit 4c1a5de) closed both competitors' OWN false-rank-1 footprints to 0/175, but did not touch either state's pre-existing loss of its own 3 dedicated profiles to `built_to_fail` (already documented as pre-existing on both states' own rows, unaffected by their re-clustering) — so that dominance simply reverted to `built_to_fail`, on top of its original 41, landing at 52. Full per-profile detail in `tools/_scdwcs_track2_reverify.py`'s output. **Any future reshape must be scoped against 52/175, not 41.** No fix proposed or numbers tested this phase. **Own live `dimensional_vector`/`SALIENCE_PROFILES`/`descriptive_prose` pulled for the first time, plus the 52-profile dimensional breakdown and reverted-profile check — see the Open Investigative Questions table's Phase 9 continuation row (2026-08-21) for full detail, not reproduced here.** |
| `the_paper_tiger` | — (paired state, not itself an "overpowers" case) | Already resolved | Paired with `built_to_fail` (rank-8) | SHIPPED | | | Own salience differentiated (aptitude down, authority + attitude up, per real descriptive_prose: documentation/accountability gap). Tie with `built_to_fail` broken. Still occasionally loses to `built_to_fail` on `built_to_fail`'s own turf — expected, untouched by this fix. |
| `the_fracture` | — (declined) | Already resolved | Paired with `decision_blindness` (rank-9) | DECLINED | | | Real tie, but accurately authored given the actual text — no textual basis found for the tested differentiation hypothesis. Both states already route to the same `resolution_family`, so the tie has low practical stakes regardless. Not prioritized. |
| `decision_blindness` | — (declined) | Already resolved | Paired with `the_fracture` (rank-9) | DECLINED | | | Same reasoning as `the_fracture` — see above. **RIPPLE-AUDIT COMPLETENESS NOTE (Program Phase 6, no disposition change):** `planning_authority_gap` (this session's rank-5 pilot) now beats 2 of `decision_blindness`'s own 3 profiles outright (`ALL-DB-02/03`) — new, previously undocumented anywhere. Own pass status confirmed unaffected (still 3/3). The DECLINED disposition was never about `decision_blindness` winning everything on its own merits (it was a real-tie-accurately-authored call against `the_fracture` specifically) — this doesn't contradict that finding, it just completes the record with a fact that didn't exist at DECLINE time. |
| `the_uninitiated` | 3 — Steals from a whole other group | Cross-cluster asymmetry (explained by Program Phase 2's theory, shared rank-2 property not individual) | rank-2, full 10-way tie (own internal question, Phase 5) vs. rank-3 (real target, Phase 2) | DIAGNOSED | | | Program Phase 5 re-confirmed live (tools/_scdwcs_decomposition_rank2.py): rank-2's own 10-way tie fully live (45/45 pairs, 175/175 each). Cross-cluster theft against rank-3 now measured at **11 outbound wins, down from the originally-characterized 16** — most plausibly explained by `the_unsolved_problem`'s re-clustering out of rank-3 this session removing 3 of its former targets, but NOT chased down further; logged as unreconciled, not confirmed. All 11 are masked-shared-signal wins (all 9 other rank-2 states carry the byte-identical score) — confirms the dominance belongs to rank-2's whole vector shape, not `the_uninitiated` individually, same refinement already found for `the_second_close` in rank-6. `the_uninitiated` itself loses all 3 of its OWN dedicated profiles to Track 2 (`built_to_fail` x2, `invisible_performance_management` x1) — see the rank-2-wide finding on `decision_paralysis`'s row for full cluster context. **RE-VERIFIED, Phase 9 (2026-08-21), this session — the "17/175" global false-rank-1 figure (recorded on the Phase 8 closeout row) CONFIRMED UNCHANGED, not stale**, despite sharing the exact same measurement provenance as `invisible_performance_management`'s stale "44" and `built_to_fail`'s stale "41" (all six Track 2 census numbers came from one single execution of `tools/_scdwcs_full_hierarchy_measurement.py`, in the window after `paper_shield`/`the_unsolved_problem` re-clustered onto Aptitude but before their own later Phase 8 mitigation). Re-measured with the same corrected methodology (purpose-built sweep + independent harness `build_confusion_matrix()` cross-check, both agreeing exactly): still 17/175 (9.7%), same 8 true-target states, same per-profile detail. **Why this one didn't drift, unlike `built_to_fail`:** `the_uninitiated` is Authority-dominant (rank-2 cluster); its false-rank-1 pool (`decision_paralysis`, `disparate_impact_architecture`, `heard_and_ignored`, `hr_capture`, `sequential_decision_blindness`, `the_founders_grip`, `the_lost_map`, `the_tolerated_violation`) is entirely Authority-axis — zero overlap with the Aptitude-axis re-clustering/mitigation that moved `built_to_fail`'s and `invisible_performance_management`'s numbers. Full detail: `tools/_scdwcs_track2_reverify.py`. No fix proposed. **FULL DIAGNOSTIC PULL, Phase 9 continuation (2026-08-21):** `dimensional_vector` — authority_liability=0.45/authority_asset=0.15 (dominant, standard 3:1 shape, floor=0.15 elsewhere), matching the row's own described shape exactly. `SALIENCE_PROFILES`: standard flat authority-primary template (2.5/2.5 dominant, 0.4/0.4 elsewhere). `resolution_family`: "Intervention". `descriptive_prose` verbatim: "A significant organizational event is underway, and the people leading it have never done this before. They are capable in general and unprepared for this specific kind of decision, which means the costliest mistakes are the ones nobody on the team knows to watch for." Cluster-mate check (exact `dimensional_vector` match): **9 states** — `leadership_continuity_risk`, `decision_paralysis`, `the_policy_lag`, `dueling_narratives`, `transition_paralysis`, `the_lost_map`, `pay_exposure`, `the_pay_fog`, `compression_crisis` — exactly the other 9 members of the already-documented rank-2 10-way tie, confirming the cluster-mate methodology against a known-correct answer. **Dimension breakdown of its real 17 false-rank-1 profiles: 100% Authority (17/17)** — a fundamentally different pattern from `invisible_performance_management`/`built_to_fail`'s genuinely cross-dimensional footprint (both spread 4 ways across Attitude/Authority/Alliance/Aptitude); `the_uninitiated`'s dominance never leaves its own dimension. **CONFIRMS the standing "cross-cluster asymmetry, not a broad attractor" characterization — this row's own top-line label was correct and needed no revision.** New precision on composition: of the 17, **11 are genuine external wins against rank-3** (`the_founders_grip` 3, `disparate_impact_architecture` 3, `heard_and_ignored` 2, `hr_capture` 1, `the_tolerated_violation` 1, `sequential_decision_blindness` 1 — matches this row's already-recorded "11 outbound wins" figure exactly, independently re-confirmed) and **6 are internal-tie wins against `decision_paralysis` (3) and `the_lost_map` (3), its own rank-2 cluster-mates** — a newly-discovered mechanism, not previously counted this way: see `decision_paralysis`'s own row for the correction (those two states no longer win their own profiles via the tie; `the_uninitiated` does now). Both pieces were already correctly included in the 17/175 total confirmed earlier this session — this is a precise breakdown of that total's composition, not a change to the number itself. |
| `the_unexamined_algorithm` | 3 (partial) / smaller-scale 2 | Cross-cluster asymmetry (undiagnosed) | No cluster of its own (unique vector). Genuine wins spread: rank-3 (55%), rank-2 (36%), rank-9 (9%) | DIAGNOSED | | | Zero tie-artifacts possible (no cluster-mate to tie with) — all 11 stolen profiles are genuine wins, just spread across three different clusters rather than concentrated on one. A smaller, milder version of the "overpowers" shape. |
| `the_second_close` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-6, full 3-way tie w/ `silosolation`, `the_arbitrary_standard` | DIAGNOSED | Confirmed Alliance-correct — a specific relationship/agreement renegotiated once already, trust eroding on retry. Purely relational, distinct from its two cluster-mates. | Alliance | RE-VERIFIED this session (tools/_scdwcs_decomposition_rank6.py, direct decomposition, not the theft-shape heuristic): the previously-reported 5/175 dominance signal is NOT `the_second_close`'s own property — `silosolation` and `the_arbitrary_standard` carry the byte-identical winning score on both of the 2 genuine external wins (vs. `the_fracture`, rank-9, already DECLINED, low-stakes). `the_second_close` is simply registered earlier in engine/data/states.py, so it wins every exact tie by insertion order — this is a shared rank-6 signal, not evidence of a systematic cross-cluster asymmetry like `the_uninitiated`'s. Fix-type flag corrected accordingly. Total rank-6 cross-cluster footprint confirmed small and fully accounted for: 2 external genuine wins + 3 internal tie-artifacts, nothing larger hiding. **CORRECTION, Program Phase 7:** this row previously claimed `the_second_close` "remains fully tied with `the_arbitrary_standard` (175/175, unchanged, confirmed)" — no longer true. `the_arbitrary_standard`'s own Authority secondary has since shipped (0.4 -> 2.0), breaking that tie; `the_arbitrary_standard` now wins their shared profiles (per the margin search verified before shipping — see `the_arbitrary_standard`'s row for the full fix detail, text basis, and margin-search rationale, not duplicated here). `the_second_close`'s own 3 dedicated profiles are unaffected — confirmed zero reverse-direction ripple, `the_second_close` stays ahead on all three at every magnitude tested during that search. Still wins `silosolation`'s own dedicated profiles outright even after silosolation's Authority secondary was raised — see `silosolation`'s row for that mechanism, unrelated to and unaffected by this change. **RE-VERIFIED, Phase 9 (2026-08-21), this session — the "5/175" global false-rank-1 figure (recorded on the Phase 8 closeout row) CONFIRMED UNCHANGED, not stale**, despite sharing the exact same measurement provenance as `invisible_performance_management`'s stale "44" and `built_to_fail`'s stale "41" (all six Track 2 census numbers came from one single execution of `tools/_scdwcs_full_hierarchy_measurement.py`, in the window after `paper_shield`/`the_unsolved_problem` re-clustered onto Aptitude but before their own later Phase 8 mitigation). Re-measured with the same corrected methodology (purpose-built sweep + independent harness `build_confusion_matrix()` cross-check, both agreeing exactly): still 5/175 (2.9%), same composition — all 3 of `silosolation`'s own dedicated profiles (ALL-SI-01/02/03) plus 2 of `the_fracture`'s (ALL-FR-01/02), consistent with this row's own already-documented finding above that neither pairing was touched by the Phase 7/8 work. **Why this one didn't drift, unlike `built_to_fail`:** `the_second_close` is Alliance-dominant (rank-6 cluster); its false-rank-1 pool (`silosolation`, `the_fracture`) is entirely Alliance-axis — zero overlap with the Aptitude-axis re-clustering/mitigation. Same mechanism as `the_uninitiated`'s unaffected status, different axis. Full detail: `tools/_scdwcs_track2_reverify.py`. No fix proposed. **FULL DIAGNOSTIC PULL, Phase 9 continuation (2026-08-21):** `dimensional_vector` — alliance_liability=0.45/alliance_asset=0.15 (dominant, standard 3:1 shape, floor=0.15 elsewhere), matching this row's own described shape exactly. `SALIENCE_PROFILES`: standard flat alliance-primary template (2.5/2.5 dominant, 0.4/0.4 elsewhere). `resolution_family`: "Development + Intervention". `descriptive_prose` verbatim: "A relationship or agreement was renegotiated once already, and the same underlying issue that forced the first renegotiation is resurfacing. Whatever the first fix addressed, it wasn't the actual cause. The people involved are less willing to extend trust a second time." Cluster-mate check (exact `dimensional_vector` match): **2 states** — `silosolation`, `the_arbitrary_standard` — exactly its already-documented rank-6 3-way tie, confirming the cluster-mate methodology again. **Dimension breakdown of its real 5 false-rank-1 profiles: 100% Alliance (5/5)** — same narrow, single-dimension pattern as `the_uninitiated`, not a broad cross-dimensional attractor. Composition unchanged from what this row already documents: 3 from `silosolation` (own cluster-mate, tie-order win, mechanism unchanged and confirmed stable — unlike `the_uninitiated`'s tie-break reversal, `the_second_close` is still the one winning this specific tie, no flip detected) + 2 from `the_fracture` (genuine external win, already documented). **CONFIRMS the standing narrow-footprint characterization for this state too.** |
| `culture_drift` | 3 (partial), smaller scale | rank-11 internal: SAME-CLUSTER DIFF (confirmed, not prioritized). Cross-cluster vs. rank-1/rank-10: explained by Program Phase 2's working theory, not "undiagnosed" anymore | rank-11 (own cluster, tie w/ `wellbeing_theater`) vs. rank-1 (75% of genuine wins) and rank-10 (25%) | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4 | Confirmed genuinely Attitude-dominant, distinct from `wellbeing_theater` — a gradual, undecided values-reward drift, no mis-clustering question. Not prioritized, low practical stakes. | Attitude | Program Phase 3 (prompts/scd-wcs-remediation-program-v1.md). Re-verified live: rank-11 tie still fully live (175/175 tied, both `dimensional_vector` and `SALIENCE_PROFILES` byte-identical to `wellbeing_theater`). Cross-cluster theft against rank-1/rank-10 still 1 tie-artifact + 4 genuine external wins as previously recorded — now mechanistically explained by Phase 2's working theory (elevated Authority secondary captures real off-axis signal that `the_broken_compass`'s pure Attitude vector misses), not applied as a fix here, this phase only confirms the explanation holds. **PRACTICAL STAKES CHECK (same shape as rank-4):** of rank-11's 6 own dedicated profiles, only 1 (`EXP-WT-02`) is a case where internal differentiation would even matter — the other 5 are already lost to external states regardless of internal tie-break credit. Both states route to the identical `resolution_family` ("Intervention") — a corrected internal attribution would not change any user-facing recommendation. **NEW FINDING, flagged clearly:** `paper_shield`'s re-clustering (shipped earlier this session, Authority -> Aptitude) already changed the true winner on 3 of rank-11's 6 own dedicated profiles (`ATT-CD-02`/`ATT-CD-03`/`EXP-WT-01`) — a real ripple effect from an unrelated fix, fully masked by the lenient `pass_criterion` (both states still show 3/3), not a new problem but a general caution: future `dimensional_vector` moves (especially Phase 8's Track 2 work) should check for this kind of cross-state ripple directly, not just trust the aggregate 171/175 count. **PHASE 8 SUPERSEDES, verified against live pipeline:** `paper_shield`'s own Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `ATT-CD-02`/`ATT-CD-03` now go to `invisible_performance_management`, `EXP-WT-01` now goes to `the_unformed_leader`. Own pass status unaffected (still 3/3). **Text basis for the eventual fix, if ever piloted:** "Nobody decided to abandon the values. They just stopped being what got rewarded" is an explicit disclaimer against deliberate decision-making — the same disclaiming shape as `silosolation`'s "isn't hostile" and `the_arbitrary_standard`'s "even if nobody designed it on purpose" — argues for lowering `culture_drift`'s Authority secondary toward flat, keeping `wellbeing_theater`'s (whose text implies an active choice about what to address). **Search already run and confirmed clean** (tools/_salience_pilot_search_rank11.py, 6 real-pipeline candidates): every candidate breaks the tie (0/175), every own moderate profile passes with healthy margin (+0.20 to +0.26), best floor at cd_auth=0.70/wt_auth=1.50 (0.000254) — available for Phase 8 or a future session, not executed now. **Phase 2 side-effect explicitly checked:** `culture_drift`'s win margin against `the_broken_compass` barely moves under any tested candidate (0.9704 -> 0.9696) — no threat to the Phase 2 cross-cluster finding if this is ever piloted. |
| `invisible_performance_management` | 2 — Overpowers everything | Broad/cross-cluster (Track 2) | No cluster of its own (unique vector). Broad — 44/175 (25.1%) as of Phase 8's re-measurement, down from the original 59/175 (33.7%); zero true rank-1 ever | DIAGNOSED, searched (Phase 8), not fixed | | | The single largest dominance problem in the whole taxonomy — bigger than `built_to_fail`. No cluster-mate, no tie to break — pure vector-strength dominance. **Program Phase 8 searched this** (own-salience sweep + joint combinations with `built_to_fail`/`the_unsolved_problem`): its false-rank-1 footprint IS salience-reducible in isolation (44 -> 0 by magnitude 1.0), but it never wins even its own 3 dedicated profiles at ANY tested salience or combination — its own vector's real dominant signal (Attitude/Authority, per raw `accumulated_vector` magnitude, not just `dimension_summary`'s ambiguous asset-ratio) doesn't even concentrate on its own authored Aptitude axis, a deeper misalignment than a pure over-strength problem. No fix shipped — see the Open Investigative Questions table's Phase 8 closeout row for the full joint finding. **PHASE 9 DIAGNOSTIC PASS (2026-08-21), this session:** full live data pull (dimensional_vector, SALIENCE_PROFILES, resolution_family, descriptive_prose, cluster-mate check) re-confirmed against the current codebase, no fix proposed. **False-rank-1 count CORRECTED to 59/175 (33.7%)** — this row's prior "44/175" figure was a stale intermediate measurement (taken after paper_shield/the_unsolved_problem's re-clustering but before their own later Phase 8 salience mitigation), never re-verified after that mitigation shipped, unlike every other affected row in this tracker. Own dedicated profiles (EXP-IPM-01/02/03) reconfirmed: all 3 lose outright to `built_to_fail` specifically (own_rank=2 every time), not a mix of attractors. See the Open Investigative Questions table's Phase 9 row for the full correction, the 32-state axis breakdown, and the raw data source. **Phase 9 continuation (2026-08-21): field-by-field vector/salience/prose comparison against its top-10 highest-volume true-target competitors now pulled — see the Open Investigative Questions table's Phase 9 continuation row for the full comparative overlap analysis, not reproduced here.** **Phase 9 continuation-2 (2026-08-21): the top-10 sample extended to a FULL 32-state overlap classification, plus a sandbox concentration/skew mechanism test — see the Open Investigative Questions table's Phase 9 continuation-2 row.** **DUAL-PROBLEM SUMMARY (2026-08-21), log-only — full detail on the rows above, not repeated here:** the 59 false-rank-1 profiles split roughly 61% concentration-attributable / 39% field-collision-attributable — a genuine TWO-part problem, not one. Concentration alone (holding field content fixed) accounts for ~36 of the 59 at this state's own current skew ratio. Field collision accounts for the rest, but is NOT a clean lever either: in the Authority group specifically, `invisible_performance_management` beats several states (`pay_exposure`, `the_pay_fog`, `compression_crisis`, `planning_authority_gap`) that individually out-weigh it on the exact colliding field (authority_liability) — so simply reducing `invisible_performance_management`'s own authority_liability weight would not obviously be the fix, since the competitor already "should" win on paper and doesn't. Separately, and not fixed by either lever alone: `invisible_performance_management` never wins its own 3 dedicated profiles (EXP-IPM-01/02/03) at any tested concentration — a distinct problem from the broad false-rank-1 footprint. **Flagged explicitly: any real reshape proposal needs to address both the concentration piece and the field-collision piece together, not one in isolation, and the specific field/magnitude choices need Pete's own clinical read against the real pulled `descriptive_prose` (this row, and the Phase 9 rows above) before any candidate goes to Gemini — not proposed unilaterally by CC or Claude.ai.** |
| `the_overloaded_manager` | 4 — Unexplained | Unexplained anomaly | No cluster of its own (unique vector). Small — 4/175 (2.3%), all against attitude-dominant targets despite being aptitude-dominant itself | DIAGNOSED (anomaly, unexplained) | | | Doesn't fit any other pattern. Checked one case directly at the session-vector level — didn't explain the win either. Sample too small (n=4) to trust a theory yet. Flagged for more data. |
| `what_nobody_says` | 1 — Blends in (already resolved) | Already resolved | rank-4 (was tied with the other 5, already split out) | SHIPPED (pre-existing, before this investigation) | | | Already carries a distinct salience entry (alliance-dominant vs. the other 5's attitude-dominant) — confirmed well-grounded, a validated precedent, not an open item. |
| `identity_erosion` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-4, 5-way tie remains (`identity_erosion`, `the_culture_that_wasnt`, `narrative_lock`, `the_unreported_hazard`, `the_unlocked_door`) | DIAGNOSED — real fix deferred to Phase 8 (Track 2) | Genuinely distinct on real text (organizational-identity/differentiation loss, surfaces in retention/recruiting) — not prioritized, low practical stakes, same-cluster pilot not scheduled. | Attitude | Program Phase 1 (prompts/scd-wcs-remediation-program-v1.md). Re-verified live (tools/_scdwcs_decomposition_rank4.py): 5-way tie still fully tied 175/175 on all 10 pairs, unaffected by this session's rank-5/rank-6/paper_shield work. Cross-cluster theft minimal — 1 profile in the whole suite (ATT-GD-01, targeting `groundhog_day`) — and it's the same masked-shared-signal mechanism as rank-5/rank-6: all 5 rank-4 states carry the byte-identical winning score, `identity_erosion` is just credited first by registration order, not its own individual property. **ROOT-CAUSE FINDING:** `identity_erosion`'s standing pre-existing calibration failure (one of the 4 tracked all session, alongside `invisible_burnout`/`leadership_deafness`/`the_untouchable`) is caused by Track 2 domination — it loses its own profiles outright to `built_to_fail`/`invisible_performance_management` (e.g. ATT-IE-02: 0.895 vs. 0.314), a large real gap, not a rank-4-intrinsic problem. Retire the "unexplained" framing on this failure going forward — it's explained, just not yet fixed (Phase 8 scope). |
| `the_culture_that_wasnt` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-4, same 5-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2) | Genuinely distinct on real text (hiring-promise-vs-reality mismatch, discovered by new hires in their first few months) — not prioritized, low practical stakes. | Attitude | See `identity_erosion`'s row for the shared rank-4 tie/theft findings. Own dedicated profiles (ATT-TCWW-01/02/03) lose outright to `built_to_fail` by a large margin (0.932 vs. 0.718) — same Track 2 pattern already logged for `the_arbitrary_standard`/`the_second_close`/`paper_shield`, currently masked by a lenient `pass_criterion` (still counts 3/3). Not currently a counted failure, unlike `identity_erosion`. |
| `narrative_lock` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-4, same 5-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2); distinct fragility logged separately | Genuinely distinct on real text (self-narrative rigidity, punishes truth-tellers) — not prioritized, low practical stakes. Its own `resolution_family` ("Executive Counsel + Intervention") differs from the other 4. | Attitude | See `identity_erosion`'s row for the shared rank-4 tie/theft findings. **DISTINCT FINDING, not Track 2:** own dedicated profiles (ATT-NL-01/02/03) lose to `the_dormant_talent` (rank-7, Aptitude) instead — a single-rival near-miss, not a broad attractor. Margin on 2 of 3 profiles is razor-thin (0.951888 vs. 0.951290, a gap of 0.0006). See the Open Investigative Questions table below for the standalone fragility flag — not scoped further this phase. |
| `the_unreported_hazard` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-4, same 5-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2) | Genuinely distinct on real text (safety-reporting suppression from learned futility, not a process gap) — not prioritized, low practical stakes. | Attitude | See `identity_erosion`'s row for the shared rank-4 tie/theft findings. Own dedicated profiles (ATT-UH-01/02/03) lose outright to `invisible_performance_management`/`built_to_fail` by a large margin (0.948 vs. 0.778) — same Track 2 pattern already logged for `the_arbitrary_standard`/`the_second_close`/`paper_shield`, currently masked by a lenient `pass_criterion` (still counts 3/3). **RIPPLE-AUDIT COMPLETENESS NOTE (Program Phase 6, no disposition change):** `the_unsolved_problem`'s re-clustering (this session) is now ALSO a competitor, winning 2 of these 3 own profiles outright (ATT-UH-02/03) — on top of the already-documented Track 2 losses, not instead of them. Same lenient-`pass_criterion` masking applies (still 3/3). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `ATT-UH-02/03` now go to `built_to_fail` instead. Own pass status unaffected (still 3/3). |
| `the_unlocked_door` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-4, same 5-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2) | Genuinely distinct on real text (security-practice neglect from passive drift, not active suppression) — not prioritized, low practical stakes. Its own `resolution_family` ("Development + Intervention") differs from the other 4. | Attitude | See `identity_erosion`'s row for the shared rank-4 tie/theft findings. Own dedicated profiles (ATT-UD-01/02/03) lose outright to `invisible_performance_management`/`built_to_fail` by a large margin (0.948 vs. 0.778) — same Track 2 pattern already logged for `the_arbitrary_standard`/`the_second_close`/`paper_shield`, currently masked by a lenient `pass_criterion` (still counts 3/3). **RIPPLE-AUDIT COMPLETENESS NOTE (Program Phase 6, no disposition change):** `the_unsolved_problem`'s re-clustering (this session) is now ALSO a competitor, winning 2 of these 3 own profiles outright (ATT-UD-02/03) — on top of the already-documented Track 2 losses, not instead of them. Same lenient-`pass_criterion` masking applies (still 3/3). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `ATT-UD-02/03` now go to `built_to_fail` instead. Own pass status unaffected (still 3/3). |
| `the_untouchable` | — (already resolved) | Already resolved | rank-10, paired with `the_inner_circle` | SHIPPED (pre-existing, before this investigation) | | | Already carries a distinct salience entry — confirmed well-grounded this session (individual exemption vs. systemic clique, a real narrative distinction). **RIPPLE-AUDIT COMPLETENESS NOTE (Program Phase 6, no disposition change):** `paper_shield`'s re-clustering (this session) now wins ALL 3 of `the_untouchable`'s own dedicated profiles outright (`ATT-UT-01/02/03`) — new external competitive pressure, previously only the aggregate-pass-count-unaffected fact was known. Confirmed the counted pass status stays at its standing 2/3 (see the new `invisible_burnout`-adjacent fragility row, Open Investigative Questions, for the related compounding-pressure concern). This is about a new EXTERNAL competitor, not about `the_untouchable`'s validated differentiation from `the_inner_circle` — that relationship is unaffected. **PHASE 8 SUPERSEDES, verified against live pipeline:** `paper_shield`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `ATT-UT-01/02/03` now all go to `invisible_performance_management` instead. Counted pass status unaffected (still 2/3, the same standing known failure). |
| `the_inner_circle` | — (already resolved) | Already resolved | rank-10, paired with `the_untouchable` | SHIPPED (pre-existing, before this investigation) | | | Uniform-default salience (no custom entry) is enough to break the tie against `the_untouchable`'s sharp entry. `resolution_family` match is cosmetic in substance (same two families, different order) but the differentiation itself is real and validated. **RIPPLE-AUDIT COMPLETENESS NOTE (Program Phase 6, no disposition change):** `the_unsolved_problem`'s re-clustering (this session) now wins 1 of `the_inner_circle`'s own 3 profiles outright (`EXP-IC-03`) — new, previously undocumented. Own pass status confirmed unaffected (still 3/3). Same as `the_untouchable`'s note — new external competitive pressure, the pair's own validated differentiation from each other is unaffected. **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `EXP-IC-03` now goes to `invisible_performance_management` instead. Own pass status unaffected (still 3/3). |
| `wellbeing_theater` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-11, paired with `culture_drift` | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4 | Confirmed genuinely Attitude-dominant — visible wellbeing programming that avoids the real cause, an implied active-choice story distinct from `culture_drift`'s undecided drift. Not prioritized, low practical stakes. | Attitude | See `culture_drift`'s row for the full shared rank-11 tie/theft findings, the paper_shield ripple-effect finding, the search results, and the Phase 2 side-effect check. Still tied with `culture_drift` (175/175, confirmed live). Both route to the same `resolution_family` ("Intervention") — a corrected internal attribution would not change any user-facing recommendation. |
| `the_founders_grip` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-3, full 8-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4/rank-11 | Confirmed Authority-correct — single person's approval gates nearly every consequential decision, a classic bottleneck. Not prioritized, low practical stakes. | Authority | Program Phase 4 (prompts/scd-wcs-remediation-program-v1.md). Re-verified live (tools/_scdwcs_decomposition_rank3.py): full 8-way tie still fully tied 175/175 on all 28 pairs. **RANK-3-WIDE FINDING, this row carries it for all 7 remaining states (`the_exposed`, `hr_capture`, `heard_and_ignored`, `the_tolerated_violation`, `sequential_decision_blindness`, `disparate_impact_architecture` all reference back here):** zero outbound wins anywhere in the 175-profile suite — rank-3 never wins a single profile outside itself. **100% of the cluster's own 24 dedicated profiles (8 states x 3) lose their own rank-1 slot to an external competitor** — `the_uninitiated` wins 12/24, `the_unexamined_algorithm` 6/24, `invisible_performance_management` (Track 2) 4/24, `planning_authority_gap` 2/24 (the latter now correctly attributed as a genuine new ripple from the rank-5 pilot, not a tie-surfacing artifact — see `planning_authority_gap`'s own row for the full correction). This is the most complete real-world confirmation of Program Phase 2's cross-cluster asymmetry theory found this session: a pure single-axis vector with zero floor/secondary elevation loses 100% of its own real-text-matched profiles, every time, to every kind of more-diffuse rival. All 8 states still show 3/3 in the standard suite — fully masked by the lenient `pass_criterion`, no new counted failures. 7 of 8 states (all but `the_unsolved_problem`, already re-clustered) share `resolution_family` "Intervention + Executive Counsel" — internal differentiation among them would not change any user-facing recommendation even if perfectly fixed, same cosmetic-value shape as rank-4/rank-11. `the_founders_grip` itself is also the single biggest share of `the_uninitiated`'s cross-cluster theft (3 of its 12 rank-3 wins). |
| `the_exposed` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-3, full 8-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4/rank-11 | Confirmed Authority-correct — no function whose actual job is managing employee-related risk, an accountability-ownership void. Not prioritized, low practical stakes. | Authority | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (AUT-EX-01/02/03) lose to `invisible_performance_management` (Track 2, x2) and `the_unexamined_algorithm` (x1). |
| `hr_capture` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-3, full 8-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4/rank-11 | Confirmed Authority-correct — the function meant to protect the organization has been repurposed to protect specific leaders, a deliberate, knowing abuse of governance power ("know exactly what they're doing"). Not prioritized, low practical stakes. | Authority | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (AUT-HC-01/02/03) lose to `the_uninitiated` (x1), `invisible_performance_management` (Track 2, x1), and `the_unexamined_algorithm` (x1). |
| `heard_and_ignored` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) | rank-3, full 8-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4/rank-11 | Confirmed Authority-correct — concerns raised through official channels are reliably not acted on, a formality rather than a corrective mechanism — an accountability/follow-through gap, not a silence gap (distinct from `what_nobody_says`, which is about not raising a concern at all). Not prioritized, low practical stakes. | Authority | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (AUT-HI-01/02/03) lose to `the_uninitiated` (x2) and `invisible_performance_management` (Track 2, x1). |
| `the_tolerated_violation` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed, not prioritized) — **genuine dual-axis candidate, distinct from the other 4's single-axis shape** | rank-3, full 8-way tie | DIAGNOSED — real fix deferred to Phase 8 (Track 2), same call as rank-4/rank-11 | Confirmed Authority-correct, but unlike its 4 single-axis cluster-mates the text explicitly names a second axis in the same sentence: "Nobody **with the authority** to stop it has been **willing** to be the one who does" — Authority AND willingness (Attitude) both named. If this cluster is ever revisited beyond Phase 8's deferred low-stakes pilot, `the_tolerated_violation` is the one state with real textual grounds for an Attitude secondary elevation, not just a same-cluster salience nudge. Not prioritized this session regardless — low practical stakes, same as the other 4. | Authority | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (AUT-TV-01/02/03) lose to `the_unexamined_algorithm` (x2) and `the_uninitiated` (x1). |
| `the_unsolved_problem` | 1 — Blends in (re-clustered, no longer rank-3) | RESOLVED (re-clustered) + Phase 8 mitigation SHIPPED | Was rank-3 (Authority, full 8-way tie, the one outlier on `resolution_family`); now genuinely unique-vector Aptitude, no cluster of its own | SHIPPED (re-clustered + Phase 8 mitigation) | Confirmed: this is Aptitude, not Authority — zero decision-rights/accountability content anywhere; text purely about inadequate root-cause-solving capability, repeated fixes that treat symptoms not causes. | Aptitude | **RE-CLUSTERED Program Phase 4** (this row was never updated at the time -- corrected now): `primary_dimension` "Authority" -> "Aptitude", `dimensional_vector` moved from authority=0.60/0.10 to aptitude=0.50/0.15 via a 9-candidate search against the real calibration pipeline (tools/_scdwcs_scoping_unsolved_problem_search.py) — 0.50 is the smallest magnitude confirmed safe (below it, threatens `the_untouchable`'s already-marginal profile; at 0.45, collides exactly with `paper_shield`'s vector). Salience held at the standard flat-aptitude-primary template throughout the search. **PHASE 8 MITIGATION (same session, later):** the taxonomy-wide census found this re-clustering manufactured a 19/175 false-rank-1 footprint that measured exactly 0/175 before it (reconstructed via `git show` and re-measured under the current taxonomy) — a real, previously-uncaught side effect of moving onto the already-crowded Aptitude axis. No secondary axis exists in the text — fresh re-read confirmed genuinely single-axis-pure, ruling out a paper_shield-style Attitude-secondary fix. Tested vector-magnitude/skew adjustment instead, in BOTH directions: sharpening the skew further (lowering `aptitude_asset`) backfires catastrophically (100/175 at 0.10, 123/175 at 0.05 plus a new regression failure) — this is shape-tuning, not narrative-grounded, and the wrong direction was tried and rejected before the correct one was found. Reducing the skew (`aptitude_asset` 0.15 -> 0.20, ratio 3.33:1 -> 2.5:1) cleanly resolves it. Margin-searched (0.16-0.25, full 175-profile suite): 0.20 is the smallest value where the binding near-miss constraint stabilizes on one profile with a margin ~7x the razor-thin low end. Verified against disk after writing: 171/175 unchanged, same 4 known failures, HC/extreme-HC OK, false-rank-1 confirmed 0/175 with both this fix and paper_shield's fix live simultaneously. Still loses all 3 of its own dedicated profiles (AUT-UP-01/02/03) to `built_to_fail` — separate, already-known Track 2 problem, unaffected by and not solved by this fix. |
| `sequential_decision_blindness` | 1 — Blends in | THREE-WAY MISMATCH — not a clean re-clustering candidate | rank-3, full 8-way tie | DIAGNOSED — scoped this session, no fix recommended | Text reads Alliance (coordination failure); current vector is Authority; real `dimension_summary` on its own 3 calibration profiles is Attitude-dominant (0.46-0.51) — an axis with zero textual support either. None of the three agree — closest precedent is `the_fracture`/`decision_blindness`'s DECLINED disposition (real tie, tested hypothesis doesn't hold), not `paper_shield`/`the_unsolved_problem`'s clean re-clustering pattern. | Unresolved — Authority (current), Alliance (text), and Attitude (real data) all disagree | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (EXP-SDB-01/02/03) lose to `the_uninitiated` (x1) and `planning_authority_gap` (x2 — the confirmed-genuine-ripple pair, see `planning_authority_gap`'s row). **SCOPED THIS SESSION (tools/_scdwcs_scoping_sdb_search.py), 8 real-pipeline candidates, Alliance NOT recommended:** text genuinely reads as a cross-functional coordination failure ("different people, without coordination... no single decision-maker intended that outcome") — same family as `the_fracture`/`decision_blindness`/`silosolation`. But every tested Alliance-elevation candidate showed the state's own score DECREASING monotonically as Alliance weight rose (0.658 -> 0.591 on EXP-SDB-01 across the tested range), crossing into a GENUINE new counted failure — not masked theft-table noise — at magnitude >=0.50 (2/3, then 1/3 at higher magnitudes). Traced mechanically: real `dimension_summary` on all 3 own profiles shows Attitude as the dominant real signal (0.4624-0.5122), Authority second (0.336-0.378), Alliance is actually the SMALLEST non-zero component (0.139) — barely above Aptitude (0.089). Raising Alliance weight pulls score away from where the real signal concentrates, same self-normalization mechanism as `the_arbitrary_standard`'s failed Attitude candidate. **Attitude explicitly rejected too, proactively, not found the hard way:** despite being the dimension_summary's strongest real component, the text carries zero willingness/mindset content — "individually defensible decisions... no single decision-maker intended that outcome" is the opposite of an attitude/willingness story. Chasing the numeric signal here would be the same trap caught twice already this session (`silosolation`'s withdrawn Authority-for-numbers temptation, `the_arbitrary_standard`'s actual Attitude failure) — not repeated a third time. **OPEN QUESTION, logged not investigated further:** possible calibration-profile-authoring gap specific to this state's own 3 test cases — the generated signal doesn't match the state's own narrative intent on any axis, which is distinct from the `dimensional_vector`/`SALIENCE_PROFILES` authoring gap this whole project addresses. Flagged for a future session, not scoped now. No vector or salience change proposed or written. Shares rank-3's dominant `resolution_family` ("Intervention + Executive Counsel") regardless — would have been truthfulness-only even if a fix had been found. |
| `disparate_impact_architecture` | 1 — Blends in | GENUINELY UNRESOLVED — data favors current clustering, text still pulls the other way | rank-3, full 8-way tie | DIAGNOSED — scoped this session, no fix proposed, genuinely unresolved | Not a confirmed-correct disposition — an open question, not a settled "leave as-is." Real `dimension_summary` favors Authority over Alliance by a real margin on all 3 own profiles, but the textual parallel to `the_arbitrary_standard`'s confirmed Alliance framing is weakened, not eliminated. | Authority (current, data-favored) vs. Alliance (textual parallel, not dissolved) — unresolved | See `the_founders_grip`'s row for the shared rank-3-wide tie/theft findings. Own dedicated profiles (EXP-DIA-01/02/03) lose to `the_uninitiated` (x3). **SCOPED THIS SESSION, dimension_summary checked BEFORE any vector search (this session's lesson applied proactively, per `sequential_decision_blindness`'s row):** real signal on all 3 profiles is Authority 0.290-0.324, Alliance 0.167-0.250, Attitude 0.390-0.480, Aptitude 0.082-0.141. **Attitude explicitly rejected**, same disclaiming-language pattern already caught twice this session — "Neutral **intent** doesn't change what the data shows" structurally mirrors `silosolation`'s "isn't hostile" and `the_arbitrary_standard`'s "even if nobody designed it on purpose," an explicit disclaimer against intent/mindset-driven readings, not a green light to chase the dominant numeric signal a third time. **Authority beats Alliance by a real margin on every profile (0.290-0.324 vs. 0.167-0.250) — not noise, and searching Alliance against this signal would repeat the exact mistake `sequential_decision_blindness` just caught.** But the textual case for Alliance isn't fully dissolved by this: `the_arbitrary_standard`'s confirmed framing is about "how people are treated relative to each other," and "a policy... produces meaningfully different outcomes across different groups" is arguably still that same concern by this taxonomy's own established convention, just surfacing through a facially-neutral-policy mechanism instead of informal inconsistency. Checked `the_diversity_ceiling` (an existing Attitude-dominant equity-themed state, "stated commitment... visible in messaging and invisible in outcomes") as a possible closer precedent — a genuinely different mechanism (performative say-vs-do promotion ceiling, not a neutral-policy-with-disparate-outcome finding), not a match. No vector search run, no fix proposed — the disqualifying data signal on Alliance and the surviving textual pull in the same direction leave this genuinely open, not resolved either way. Flagged for a future session. Shares rank-3's dominant `resolution_family` ("Intervention + Executive Counsel") regardless — would be truthfulness-only even if a fix were eventually found, same as `sequential_decision_blindness`. |
| `leadership_continuity_risk` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) — **REAL consequential stakes, not cosmetic** | rank-2, full 10-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), but flagged higher-priority than rank-3/4/11's deferrals | Confirmed Authority-correct — concentrated succession risk, no defined replacement path. | Authority | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings and the consequential-stakes note. Own dedicated profiles (AUT-LC-01/02/03) lose to `the_unexamined_algorithm` (x1) and `built_to_fail` (x2). `resolution_family` "Roadmap + Development" — real, distinct from most of its cluster-mates. |
| `decision_paralysis` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) — **genuine internal-tie WINNER, distinct pattern** | rank-2, full 10-way tie | DIAGNOSED — deferred to Phase 8 (Track 2) for a full fix, but already winning today via the tie itself | Confirmed Authority-correct — governance structure that doesn't produce clear decision ownership. | Authority | Program Phase 5 (prompts/scd-wcs-remediation-program-v1.md). **RANK-2-WIDE FINDING, this row carries it — `the_uninitiated`, `leadership_continuity_risk`, `the_policy_lag`, `dueling_narratives`, `transition_paralysis`, `the_lost_map`, `pay_exposure`, `the_pay_fog`, `compression_crisis` all reference back here:** full 10-way tie re-verified live (45/45 pairs tied, 175/175 each). Unlike rank-3's 100% external-loss rate, **`decision_paralysis` and `the_lost_map` genuinely WIN their own 3 dedicated profiles outright** — own_score exactly equals the true winner's score on all 6 profiles combined (e.g. AUT-DP-01: 0.985507 = 0.985507), confirming they win via the internal tie itself, not lost to anything external. The other 8 states lose their own profiles externally (mostly Track 2, plus `the_uninitiated` and `the_unexamined_algorithm`), same lenient-`pass_criterion`-masked pattern as rank-3/4/11 (all still show 3/3). **CONSEQUENTIAL STAKES ARE REAL HERE, unlike rank-3/4/11:** `resolution_family` spread across the 10 states is genuinely diverse — 6 distinct combinations (`Intervention`; `Roadmap + Development` x2; `Roadmap + Intervention`; `Roadmap` x4; `Executive Counsel + Roadmap`; `Intervention + Roadmap`) — confirming the original "5 distinct combos" characterization. A real fix here (once the standing methodological confound below is resolved) would have genuine downstream effect on user-facing recommendations, not just correctness value. **THIRD CONFIRMED RIPPLE EFFECT this session:** `the_unsolved_problem`'s re-clustering (Authority -> Aptitude, Phase 4) now beats `transition_paralysis` (2 of 3 own profiles) and `dueling_narratives` (1 of 3) — see `the_unsolved_problem`'s own row for the fix detail. Phase 2's theory applied: rank-2's floor=0.15 (vs. rank-3's floor=0.10) is exactly the shape difference the theory predicts should win on attack, and it does (11/11 confirmed); on defense, the theory doesn't fully protect rank-2 against Track 2 or `the_unsolved_problem`'s newly-diffuse vector, consistent with the pattern everywhere else this session — floor shape doesn't help against genuine magnitude dominance. **CORRECTION, Phase 9 (2026-08-21), this session — the bolded claim above is NO LONGER TRUE, caught while re-verifying `the_uninitiated`'s false-rank-1 detail:** `decision_paralysis` and `the_lost_map` do NOT currently win their own 3 dedicated profiles. Directly re-checked (AUT-DP-01/02/03, AUT-LM-01/02/03): the internal 10-way tie is still fully live and byte-identical (confirmed again this session via a fresh cluster-mate exact-vector-match check), but the specific tie-break WINNER has shifted — `the_uninitiated` now wins that exact tied score on all 6 of these profiles (e.g. AUT-DP-01: `the_uninitiated`'s score 0.985507 = `decision_paralysis`'s own_score 0.985507, but `decision_paralysis` now shows own_rank=3, not rank=1; `the_lost_map` similarly now shows own_rank=7). Neither state's own counted pass/fail status changed (the lenient prominence criterion still passes a tie either way, so the regression suite's 171/175 never moved and nobody would catch this without a direct per-profile check) — but the specific mechanistic claim in this row ("genuinely WIN their own 3 dedicated profiles outright") is now false. **Not root-caused this phase** — cause of the tie-break-winner shift (some ordering/registration change, timing unknown, possibly incidental to one of this session's own re-clustering or salience edits, or possibly already stale before this session started) is flagged as a new open question, not chased down further. See `the_uninitiated`'s own row and the Open Investigative Questions table's Phase 9 continuation-2 row for the full detail — these 6 profiles are already correctly counted within `the_uninitiated`'s real 17/175 false-rank-1 total, not a new/additional discovery, just a newly-understood mechanism behind numbers already on record. |
| `the_policy_lag` | 1 — Blends in | MIS-CLUSTERING HYPOTHESIS TESTED AND REJECTED — Aptitude candidate empirically fails | rank-2, full 10-way tie | DIAGNOSED — mechanically falsified, not "no viable axis found" | Text echoes `paper_shield` (stale documentation, "practice has moved on without the documentation catching up") — a real, strong textual candidate for Aptitude. But the real weight-raise test (not just the raw `dimension_summary` ranking) shows own_score DECREASING monotonically at every tested magnitude. | Authority (current) — Aptitude candidate empirically rejected | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings. **FULL MECHANICAL FALSIFICATION TEST RUN** (raising `aptitude_liability`/`aptitude_asset` from baseline 0.4 up to 5.0, real pipeline, own 3 dedicated profiles AUT-PL-01/02/03): 0.40->0.958882/0.948908/0.954397, 1.00->0.921730/0.906431/0.915761, 2.50->0.865235/0.842068/0.854839, 5.00->0.820361/0.790604/0.803593 — a steep, unambiguous monotonic decline at every step, never reversing. **This is empirically different from `the_arbitrary_standard`'s "no viable axis found" outcome — here the specific text-grounded candidate WAS tested and definitively fails, not merely unproposed.** See the standing methodological finding (Open Investigative Questions table) for why the raw `dimension_summary` ranking alone couldn't have distinguished this from a real signal — the mechanical weight-raise test is what actually discriminates. |
| `dueling_narratives` | 1 — Blends in | POSSIBLE MIS-CLUSTERING (open Q) — flagged, NOT mechanically tested | rank-2, full 10-way tie | DIAGNOSED — flagged for future scoping, not tested | Text is genuinely ambiguous between Alliance ("different parts of the organization... nobody has reconciled them" — a coordination reading) and Authority (reconciliation as an ownership gap) — unlike `the_policy_lag`/`the_pay_fog`, no single strongest candidate to test first. | Unresolved — Authority (current), Alliance (partial textual pull) | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings. Own dedicated profiles (AUT-DN-01/02/03) lose to `invisible_performance_management` (Track 2, x1), `built_to_fail` (Track 2, x1), and `the_unsolved_problem` (x1 — the third confirmed ripple, see `decision_paralysis`'s row). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `AUT-DN-03` now also goes to `invisible_performance_management`. **NOT run through the mechanical falsification test this phase** — `the_policy_lag`/`the_pay_fog`'s negative results make a positive outcome here unlikely (same generator-level `dimension_summary` confound applies, see the standing methodological finding below), but that's an inference, not a direct result for this specific state. Genuinely open, not resolved either way. |
| `transition_paralysis` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) — **REAL consequential stakes, not cosmetic** | rank-2, full 10-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), but flagged higher-priority than rank-3/4/11's deferrals | Confirmed Authority-correct, cleanest text in the cluster — explicit "uncertain which **authority** actually governs their work." | Authority | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings and the consequential-stakes note. Own dedicated profiles (AUT-TP-01/02/03) lose to `the_unsolved_problem` (x2 — the third confirmed ripple this session) and `built_to_fail` (Track 2, x1). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `AUT-TP-01` now goes to `built_to_fail`, `AUT-TP-03` now goes to `invisible_performance_management`. `resolution_family` "Intervention + Roadmap" — real, distinct from most of its cluster-mates. |
| `the_lost_map` | 1 — Blends in | GENUINE internal-tie WINNER (same as `decision_paralysis`) — separate open mis-clustering question, NOT mechanically tested | rank-2, full 10-way tie | DIAGNOSED — winning today via the tie; Aptitude echo flagged not tested | Confirmed Authority-correct enough to win today (see below), but the text ("institutional knowledge lives in individual heads rather than in any system") echoes `the_policy_lag`'s Aptitude candidate closely — the state's own-tie-winner status is a SEPARATE question from whether that Aptitude echo is real. | Authority (current, winning) — Aptitude echo unresolved | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings. Own dedicated profiles (AUT-LM-01/02/03) genuinely WIN outright via the internal tie (own_score = true winner's score on all 3, e.g. 0.976972 = 0.976972) — one of only 2 of 10 rank-2 states with this property. **The Aptitude mis-clustering question was NOT run through the mechanical falsification test this phase** — same inference-not-direct-result caveat as `dueling_narratives`, `pay_exposure`. Worth noting explicitly: if this state's own real signal is subject to the same generator-level confound as `the_policy_lag`'s (both share the strong documentation/institutional-knowledge theme), a future test would likely also fail — but this hasn't been confirmed directly, and unlike the others, `the_lost_map` currently WORKS (wins its own profiles), which raises the stakes of getting any future re-clustering attempt right rather than accidentally trading a working state for a broken one. |
| `pay_exposure` | 1 — Blends in | POSSIBLE MIS-CLUSTERING (open Q) — flagged, NOT mechanically tested | rank-2, full 10-way tie | DIAGNOSED — flagged for future scoping, not tested | Text ("compensation has drifted... discovering this reactively... rather than proactively") reads as a reactive-vs-proactive market-tracking capability gap — a plausible but weaker Aptitude echo than `the_policy_lag`'s. | Authority (current) — Aptitude candidate unresolved | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings. Own dedicated profiles (AUT-PE-01/02/03) lose to `invisible_performance_management` (Track 2, x3). **NOT run through the mechanical falsification test this phase** — `the_policy_lag`'s negative result on a stronger version of the same Aptitude theme makes a positive outcome here unlikely (see the standing methodological finding below), but that's an inference, not a direct result. Genuinely open. |
| `the_pay_fog` | 1 — Blends in | MIS-CLUSTERING HYPOTHESIS TESTED AND REJECTED — Alliance candidate empirically fails | rank-2, full 10-way tie | DIAGNOSED — mechanically falsified, not "no viable axis found" | Text echoes `the_arbitrary_standard` ("pay decisions... don't follow a consistent, defensible logic") — a real, strong textual candidate for Alliance (inconsistent-treatment framing). But the real weight-raise test shows own_score DECREASING monotonically at every tested magnitude, more gradually than `the_policy_lag`'s but never reversing. | Authority (current) — Alliance candidate empirically rejected | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings. **FULL MECHANICAL FALSIFICATION TEST RUN** (raising `alliance_liability`/`alliance_asset` from baseline 0.4 up to 5.0, real pipeline, own 3 dedicated profiles AUT-PF-01/02/03): 0.40->0.915862/0.894110/0.901471, 1.00->0.914661/0.888239/0.895259, 2.50->0.912311/0.875587/0.881851, 5.00->0.909847/0.859355/0.864604 — a gentler decline than `the_policy_lag` but unambiguously monotonic, never reversing even at 12.5x baseline. **Empirically different from `the_arbitrary_standard`'s "no viable axis found" — here the specific text-grounded candidate WAS tested and definitively fails.** See the standing methodological finding (Open Investigative Questions table) for why the raw `dimension_summary` ranking alone couldn't have distinguished this from a real signal. |
| `compression_crisis` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) — **REAL consequential stakes, not cosmetic** | rank-2, full 10-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), but flagged higher-priority than rank-3/4/11's deferrals | Confirmed Authority-correct — explicit "concentrating **decision-making** into fewer people than the work actually requires." | Authority | See `decision_paralysis`'s row for the rank-2-wide tie/theft findings and the consequential-stakes note. Own dedicated profiles (EXP-CC-01/02/03) lose to `invisible_performance_management` (Track 2, x3). Also served as this phase's CONTROL case for the standing methodological finding below — its `dimension_summary` is Attitude-dominant (0.42-0.52) despite unambiguous Authority text, confirming the confound isn't state-specific. `resolution_family` "Roadmap" — shared with 3 other cluster-mates, one of the cluster's more common combinations. |
| `paper_shield` | 1 — Blends in (re-clustered, no longer rank-5) | RESOLVED (re-clustered) | Was rank-5 (Authority, tied with `invisible_influence_architecture`/`planning_authority_gap`); now genuinely unique-vector Aptitude, no cluster of its own | SHIPPED (re-clustered) | Confirmed: this is Aptitude, not Authority — full descriptive_prose carries zero decision-rights/accountability content anywhere. | Aptitude | RE-CLUSTERED this session, `primary_dimension` "Authority" -> "Aptitude". `dimensional_vector` moved from Authority-dominant (0.35/0.15) to aptitude=0.45/0.15 (asymmetric, matching every other Aptitude-dominant state's liability>asset pattern), authority/alliance/attitude all dropped to flat 0.15 — the text carries zero secondary-axis content on any of the three. Landed on 0.45 via a 12-candidate search against the real calibration pipeline (tools/_scdwcs_scoping_paper_shield_search.py): it's the smallest magnitude confirmed safe — below it, paper_shield newly out-competed `the_untouchable` on one of its own already-marginal profiles (171/175 -> 170/175, HC 58/58 -> 57/58); at 0.45 and above, the full baseline held with zero new failures anywhere. Authority-retained candidates (0.20/0.25 secondary) were tested and rejected on both textual grounds (not supported) and empirical grounds (regressed at every magnitude tested). Salience held constant at the standard flat-aptitude-primary template (aptitude=2.5/2.5, rest=0.4/0.4, matching invisible_performance_management's own entry) across the whole search — confirmed a vector-only change is sufficient, no salience differentiation needed. Two companion edits required and applied: engine/data/validate.py's dimension-count literals (Aptitude 7->8, Authority 22->21), and web/lib/book-state-index.ts's public-site dimension mirror ("authority" -> "aptitude"). Verified against disk (not just in-memory) after writing all four files: full 175-profile regression 171/175 unchanged (same 4 pre-existing failures), HC/extreme-HC OK, validate.py back to 40/41 (only the pre-existing `cluster_id` gap remaining), dimension counts confirmed Aptitude=8/Authority=21. **STATED PLAINLY: this is a taxonomy correctness fix, not a calibration fix.** `paper_shield` still loses all 3 of its own dedicated profiles (AUT-PS-01/02/03) outright to `built_to_fail` after this change (paper_shield=0.937 vs. built_to_fail's higher fixed score) — the same already-flagged Track 2 domination pattern logged for `the_arbitrary_standard` and `the_second_close`, confirmed pre-existing (paper_shield already lost these same 3 profiles to `built_to_fail` in its old Authority-dominant configuration too, 0.899-0.901 vs. built_to_fail's 0.947-0.957 — not something this fix introduced or was ever going to close). **PHASE 8 MITIGATION (same session, later):** the taxonomy-wide census found this re-clustering separately manufactured a 9/175 false-rank-1 footprint that measured exactly 0/175 before it (reconstructed via `git show`) — distinct from the pre-existing own-profile loss above. Fresh re-read surfaced real, previously-untested Attitude content — "the organization **believes** it is prepared because the documentation says so" — a genuine false-confidence/complacency mechanism, not a disclaimer. Salience-only Attitude secondary, margin-searched (0.7-2.0, full 175-profile suite): reaches 0/175 at every tested magnitude; landed on 2.0, the best available margin in the tested range (0.0100 vs. 0.7's razor-thin 0.0034), confirmed no collision with any other state's salience entry. `dimensional_vector` untouched, Aptitude (2.5/2.5) still dominant. Verified against disk after writing: 171/175 unchanged, same 4 known failures, HC/extreme-HC OK, false-rank-1 confirmed 0/175 with both this fix and the_unsolved_problem's fix live simultaneously. |
| `invisible_influence_architecture` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-5, full 3-way tie | SHIPPED | Confirmed Authority-correct — needed same-cluster differentiation from its 2 cluster-mates, not a re-clustering question. | Authority | Real Authority match confirmed against live descriptive_prose: "the formally accountable people are not always the ones actually deciding outcomes" is a clean decision-rights story. Alliance secondary raised 1.0 -> 1.8 (informal, relationship-based influence is the substance of the narrative — "channels that don't match the org chart... who actually has to say yes"). Authority (2.5/2.5) unchanged, still dominant. Verified against the real calibration pipeline (tools/_salience_pilot_search_rank5.py, monkeypatch + run_profile, not hand-derived): the full 3-way rank-5 tie broke on every pairing (175/175 -> 0/175 for iia/pag, iia/ps, and pag/ps alike), own moderate profile AUT-IA-02 passes with wide margin (+0.1088), full 175-profile suite re-run after landing the value — 171/175, same 4 pre-existing failures unchanged. paper_shield deliberately left untouched at its shared value — separate open question, not part of this pilot. |
| `planning_authority_gap` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-5, full 3-way tie | SHIPPED | Confirmed Authority-correct — needed same-cluster differentiation. | Authority | Real Authority match confirmed: "don't hold the authority to make the decisions their plans depend on" is textbook Authority — name and text align. Alliance secondary lowered 1.0 -> 0.5 (a pure structural/procedural authority-execution gap in the text — "plans get built and then wait for approval from someone who wasn't part of building them" — zero relational content). Authority (2.5/2.5) unchanged, still dominant. Verified against the real calibration pipeline (tools/_salience_pilot_search_rank5.py): full 3-way rank-5 tie broke on every pairing, own moderate profile EXP-PAG-02 passes with wide margin (+0.2255), full 175-profile suite re-run — 171/175, same 4 pre-existing failures unchanged. Side effect noted, not hidden: 3 unrelated states' (decision_blindness, sequential_decision_blindness, the_burned_credibility) diagnostic false-rank-1 attribution shifted to planning_authority_gap. **CORRECTION, Program Phase 4:** this row originally claimed the shift was "the rank-5 tie-break surfacing a previously tie-obscured ranking elsewhere, not a new regression" for all 3 states — DEFINITIVELY DISPROVED for `sequential_decision_blindness` specifically. Reconstructed the exact pre-session salience value from `git show f11daf6` and re-ran EXP-SDB-02/03 directly: pre-session (alliance=1.0/1.0), `the_uninitiated` won outright (0.920302/0.929355 vs. planning_authority_gap's 0.913968/0.924602) — a clean win, NOT a masked tie. Lowering alliance to 0.5/0.5 counterintuitively RAISED planning_authority_gap's own score on these specific profiles (0.913968 -> 0.928804, 0.924602 -> 0.938011), which is what flipped the winner — a genuine new ripple effect caused directly by this pilot, not a tie-surfacing artifact. `decision_blindness` and `the_burned_credibility` were NOT re-verified this pass — this correction is scoped to what was actually checked, not extended to the other two by assumption. Neither state's own pass/fail status changed regardless (still 3/3 each) — the correction is about the MECHANISM claimed, not the regression-count outcome. paper_shield deliberately left untouched — separate open question, not part of this pilot. |
| `silosolation` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-6, full 3-way tie w/ `the_second_close`, `the_arbitrary_standard` | SHIPPED (honest partial fix) | Confirmed Alliance-correct — cross-team information isolation, explicitly "not hostile... structural," not about relationship history. Distinct from `the_second_close`'s relational-trust story. | Alliance | Authority secondary raised 0.4 -> 2.0 (structural/decision-visibility gap, matches this taxonomy's existing convention — e.g. `planning_authority_gap`'s identical "Operational & Structural" framing). Alliance (2.5/2.5) unchanged, still dominant. TIE-BREAK-ONLY, not a full fix, flagged not hidden: breaks the exact mathematical tie against both cluster-mates (0/175 tied on both pairings, isolated re-verification) with a real gap floor (0.001503, best of all candidates tested), own moderate profile (ALL-SI-02) passes with wide margin (+0.2550), full 175-profile regression re-run — 171/175, same 4 pre-existing failures unchanged. But `silosolation` still loses its own 3 dedicated profiles outright to `the_second_close` (untouched) — confirmed unmovable even at extreme magnitude (tested to 12.0, effectively flat past ~2.0). Traced to the weighted-cosine formula (engine/accumulation.py `_weighted_cosine_similarity`): `silosolation`'s authored authority_liability/authority_asset are only 0.15 each (dimensional_vector deliberately untouched, salience-only by design) — too small a vector-shape allocation for a salience weight alone to efficiently overcome `the_second_close`'s larger, untouched Alliance anchor (vector 0.45 x weight 2.5). Accepted as-is, same category as `built_to_fail`'s residual after rank-8 — a real fix would mean raising the authored vector itself, a bigger move than a same-cluster salience-only pilot. |
| `the_arbitrary_standard` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-6, full 3-way tie | SHIPPED | Confirmed Alliance-correct — inconsistent, non-transparent rule application producing differential treatment. A fairness/equity-in-treatment story (about how people are treated relative to each other), not a re-clustering candidate despite the Legal & Compliance liability axis. | Alliance | Program Phase 7 (prompts/scd-wcs-remediation-program-v1.md), leftover axis question from rank-6. Authority secondary raised 0.4 -> 2.0 (salience-only, `dimensional_vector` untouched — confirmed sufficient via mechanical falsification, tools/_scdwcs_phase7_arbitrary_standard.py). **Text basis:** rule-application/documentation language runs through all three dedicated profiles, not incidentally — "documented criteria" and "documentation" (x2, ALL-AS-01), "no formal documentation of the criteria differences" (ALL-AS-02), "rules apply differently to different people" (ALL-AS-03) — matches this state's own already-authored asset_axes ("Governance Discipline", "Accountability Architecture"). Real `dimension_summary` on these profiles actually ranked Attitude higher (0.73-0.76) than Authority (0.45-0.49) — correctly distrusted given the established bidirectional confound (this session's Phases 2/4/5/6 finding, 20 states across both directions); Authority was tested instead as the text-grounded candidate, not the numerically convenient one — the earlier-rejected Attitude candidate (this same row, prior session) was not re-tested. **PROPER MARGIN SEARCH RUN, not analogy to `silosolation`'s value** (tools/_salience_pilot_search_arbitrary_standard.py, 5 candidates across the full 175-profile suite): magnitude 1.0's own-profile margin against `the_second_close` is razor-thin (+0.0039 on ALL-AS-02/03, same fragility shape already flagged for `narrative_lock`/`the_dormant_talent`), 1.5 still thin (+0.0077); 2.0 is the smallest candidate clearing the thin-margin zone (+0.0115, an order of magnitude past 1.0) — landed here as smallest-safe, same discipline as rank-5's 1.8/0.5 pick, not smallest-possible or largest-available. ALL-AS-01 happens to land at exactly `silosolation`'s own shipped score (0.710304) — an independently-validated reference point, not the basis for the pick. Reverse-direction ripple checked directly: on `the_second_close`'s own 3 dedicated profiles, `the_arbitrary_standard` never overtakes it at any tested magnitude (SC stays ahead ~0.03 at every magnitude) — clean in both directions. Full 175-profile regression re-run at the shipped value: 171/175, same 4 pre-existing failures, zero new. **Already-logged Track 2 caveat still stands, unchanged by this fix:** `built_to_fail`/`invisible_performance_management` still win all 3 of `the_arbitrary_standard`'s own dedicated profiles outright at every magnitude tested (0.4 through 30.0) — this fix breaks the same-cluster tie against `the_second_close`, it does not touch the Track 2 domination problem, same category as `silosolation`'s shipped fix. **This closes rank-6 completely** — all 3 states now carry real dispositions: `silosolation` SHIPPED (honest partial fix), `the_second_close` untouched (the cluster's reference/baseline state), `the_arbitrary_standard` now SHIPPED. |
| `the_diversity_ceiling` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — stated commitment visible in messaging, invisible in outcomes, a say-vs-do performative gap. | Attitude | Program Phase 6 (prompts/scd-wcs-remediation-program-v1.md). **RANK-1-WIDE FINDING, this row carries it — `the_burned_credibility`, `invisible_burnout`, `the_basement_standard`, `the_inside_track`, `groundhog_day`, `the_wrong_reward`, `the_broken_compass`, `human_displacement_anxiety`, `motivational_architecture_failure`, `cultural_overtime` all reference back here:** full 11-way tie re-verified live (55/55 pairs, 175/175 each). **Zero outbound wins anywhere in the 175-profile suite** — rank-1 never wins a profile outside itself, `the_broken_compass`'s loss to `culture_drift` aside (the reverse direction). This is rank-3's complete-victim shape, not rank-2's attacking shape. **100% of the cluster's own 33 dedicated profiles (11 states x 3) lose their own rank-1 slot to an external competitor**, same lenient-`pass_criterion`-masked pattern as rank-3/4/11 (all 11 states still show 3/3 or their already-standing known-failure count). **Consequential stakes are REAL but UNEVEN, unlike rank-3/4/11's uniform low-stakes profile:** 5 distinct `resolution_family` combinations confirmed across the 11 states — differentiation stakes vary per state depending on which of the 5 groups it shares, not a flat cosmetic-everywhere picture. **Phase 2's theory, REFINED here:** floor/secondary elevation (rank-1's floor=0.15, identical magnitude to rank-2's) predicts relative competitiveness against a comparably-shaped rival, but does NOT guarantee real dominance if the broader dimension is too crowded — Attitude is the taxonomy's largest family (22 states, vs. Authority's 20 pre-session and Aptitude's 9), plausibly explaining why rank-1's identical floor shape produces zero outbound wins where rank-2 produced 11. **The massive ripple discoveries surfaced during this phase's own-profile check** (`the_unsolved_problem` alone winning 8 separate rank-1 profiles, `paper_shield` winning 3 more) led directly to the full ripple audit already committed separately — see that commit and the individual completeness notes on affected rows elsewhere in this tracker, not reproduced here. |
| `the_burned_credibility` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — "people stopped believing the announcements," earned skepticism from a history of failed follow-through, a workforce belief/mindset story. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (ATT-BC-01/02/03) lose to `the_dormant_talent` (x1), `planning_authority_gap` (x1 — pre-existing win, not a new ripple, see that state's own row), and `invisible_performance_management` (Track 2, x1). `resolution_family` "Intervention" — shares the smaller of the cluster's 5 groups. |
| `invisible_burnout` | 1 — Blends in | MIS-CLUSTERING HYPOTHESIS TESTED AND REJECTED — Aptitude candidate empirically fails | rank-1, full 11-way tie | DIAGNOSED — mechanically falsified; already carries a separate compounding-fragility flag | Text ("the organization's usual signals for catching the problem aren't catching it") reads as a detection-capability gap, a plausible Aptitude candidate. Real `dimension_summary` shows Authority dominant instead (0.487-0.488) — neither the hypothesis nor the raw ranking agree, and Authority has no textual support either. | Authority (current) — Aptitude candidate empirically rejected | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. **FULL MECHANICAL FALSIFICATION TEST RUN** (raising `aptitude_liability`/`aptitude_asset` from baseline 0.4 up to 5.0, real pipeline, own 3 dedicated profiles ATT-IB-01/02/03): 0.40->0.745261/0.651766/0.651766, 1.00->0.731064/0.634752/0.634752, 2.50->0.715530/0.618353/0.618353, 5.00->0.712002/0.616820/0.616820 — monotonic decline at every step, never reversing. **CROSS-REFERENCE, ripple audit (tools/_scdwcs_ripple_audit.py):** `invisible_burnout` is one of the 4 standing known pre-existing failures (2/3), and the full audit found it now also carries TWO new competitors on top of that standing issue (`paper_shield` x1, `the_unsolved_problem` x2 — see the Open Investigative Questions table's compounding-fragility row). This phase confirms there is NO rescue-by-re-clustering path via the Aptitude candidate — the state is under real competitive pressure with no easy fix available on this axis. |
| `the_basement_standard` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — explicit "nobody has been **willing** to enforce the standard," a clean willingness/attitude story, same shape as `the_tolerated_violation`'s Authority+Attitude case in rank-3. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (ATT-BS-01/02/03) lose to `the_unformed_leader` (x1) and `invisible_performance_management` (Track 2, x2). `resolution_family` "Intervention + Roadmap" — the cluster's own larger group. |
| `the_inside_track` | 1 — Blends in | **WEAK POSITIVE, IMPRACTICAL MAGNITUDE — new category, not clean-positive or clean-negative** | rank-1, full 11-way tie | DIAGNOSED — real signal confirmed, not recommended as a fix | Text ("channels that aren't the organization's stated process," advancement flowing through informal power) echoes `invisible_influence_architecture`'s confirmed "informal channels... who actually has to say yes" Authority framing. Real `dimension_summary` shows Authority dominant (0.44) but per the confound this alone isn't trustworthy — the mechanical test was run to check anyway. | Attitude (current) — Authority candidate shows genuine but weak signal | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. **FULL MECHANICAL FALSIFICATION TEST RUN, a genuinely different result from every other candidate tested this session:** raising `authority_liability`/`authority_asset` from baseline 0.4, own score dips slightly at moderate magnitude (1.0-1.5, ~0.847-0.850, just below the 0.852 baseline) then genuinely RISES beyond baseline at higher magnitude (5.0 -> 0.864, 30.0 -> 0.938) — a real, non-noise positive trend, unlike any rejected candidate's clean monotonic decrease. But it takes **~75x the standard baseline (magnitude 30 vs. the usual 0.4)** before it wins even 2 of its 3 profiles, and even at that extreme `ATT-IT-01` (its own high-confidence profile) is STILL won by `the_unformed_leader`, never fully clearing. **NOT recommended as a fix** — would be numeric force-fitting at an unreasonable magnitude, not a legitimate text-grounded pilot, despite the real underlying signal. Distinct from both `paper_shield`/`the_unsolved_problem`'s clean positive resolutions (decisive reversal at a reasonable 0.45-0.50) and `the_policy_lag`/`the_pay_fog`/`invisible_burnout`/`groundhog_day`'s clean negative rejections — a genuine third category. |
| `groundhog_day` | 1 — Blends in | MIS-CLUSTERING HYPOTHESIS TESTED AND REJECTED — Aptitude candidate empirically fails despite a strong narrative parallel | rank-1, full 11-way tie | DIAGNOSED — mechanically falsified | Text ("no mechanism for capturing what it learned... treated as a new problem rather than a repeat") closely echoes `the_unsolved_problem`'s already-shipped Aptitude re-clustering ("each fix treats the most recent symptom rather than whatever keeps regenerating it") — the strongest textual candidate of anything checked in rank-1. `resolution_family` "Roadmap + Executive Counsel" also carries the "Roadmap" signal that correlates with 6 of 7 existing Aptitude-dominant states. | Authority (current) — Aptitude candidate empirically rejected despite strong text support | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. **FULL MECHANICAL FALSIFICATION TEST RUN** (raising `aptitude_liability`/`aptitude_asset` from baseline 0.4 up to 5.0, real pipeline, own 3 dedicated profiles ATT-GD-01/02/03): 0.40->0.961336/0.891276/0.891276, 2.50->0.911307/0.821731/0.821731, 5.00->0.884482/0.793510/0.793510 — clean monotonic decline at every step, same shape as every other rejected candidate. Despite the closest narrative parallel to a confirmed success anywhere in this session's mis-clustering work, the real signal doesn't support it — a clear demonstration that text similarity to a successful precedent is not itself sufficient grounds, the mechanical test is what actually decides. |
| `the_wrong_reward` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — "people are responding **rationally** to the real incentives, not the stated ones," itself an attitudinal/behavioral-response story, not a capability or decision-rights gap despite a mild "Roadmap" component in its `resolution_family`. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (ATT-WR-01/02/03) lose to `invisible_performance_management` (Track 2, x1) and `the_unsolved_problem` (x2 — see the ripple audit). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `ATT-WR-02/03` now also go to `invisible_performance_management`. `resolution_family` "Intervention + Roadmap" — the cluster's own larger group. |
| `the_broken_compass` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed), already characterized | rank-1, full 11-way tie | DIAGNOSED — no new information this phase | Confirmed Attitude-correct, already established Phase 2/3 — "not a knowledge problem... a courage problem," explicitly disclaims Aptitude. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. **RE-CONFIRMED this phase, unchanged:** still loses cleanly to `culture_drift` on all 3 own profiles (ATT-BCP-01/02/03, own_score 0.949391 = `culture_drift`'s score exactly, identical to the original Phase 2/3 finding). No new candidate flagged, not re-litigated further. |
| `human_displacement_anxiety` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — explicit "people... are **anxious**... affecting engagement," an emotional/psychological disposition. "Silence from leadership doesn't read as reassurance, it reads as confirmation" reinforces the attitudinal read (how people interpret silence) rather than pulling toward Authority. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (EXP-HDA-01/02/03) lose to `paper_shield` (x1), `the_unsolved_problem` (x1), and `built_to_fail` (Track 2, x1) — see the ripple audit for the re-clustered-state detail. **PHASE 8 SUPERSEDES, verified against live pipeline:** both states' Phase 8 mitigations (commit 4c1a5de) have since eliminated their false-rank-1 footprints entirely — `EXP-HDA-01` now goes to `invisible_performance_management`, `EXP-HDA-02` now goes to `built_to_fail`. `resolution_family` "Development + Intervention" — a smaller group within the cluster's 5. |
| `motivational_architecture_failure` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — "people... **stopped believing** the incentives connect to anything real," explicit belief/mindset language, same shape as `the_burned_credibility`'s "stopped believing." Naming-collision with `the_wrong_reward` already documented and resolved in engine/data/states.py (distinct mechanisms: workforce-wide amotivation here vs. rational optimization for real incentives there) — not a new question. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (EXP-MAF-01/02/03) lose to `paper_shield` (x1), `the_unformed_leader` (x1), and `invisible_performance_management` (Track 2, x1). **PHASE 8 SUPERSEDES, verified against live pipeline:** `paper_shield`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `EXP-MAF-01` now also goes to `the_unformed_leader`. `resolution_family` "Intervention + Roadmap" — the cluster's own larger group. |
| `cultural_overtime` | 1 — Blends in | SAME-CLUSTER DIFF (confirmed) | rank-1, full 11-way tie | DIAGNOSED — deferred to Phase 8 (Track 2), same call as rank-3/4/11 | Confirmed Attitude-correct — "an unstated cultural expectation... **without having decided**, on paper, that it wants to run this way" — the same disclaiming pattern already found in `culture_drift`/`silosolation` that points away from Authority/decision-rights toward an emergent cultural read. | Attitude | See `the_diversity_ceiling`'s row for the shared rank-1-wide tie/theft findings. Own dedicated profiles (EXP-CO-01/02/03) lose to `the_overloaded_manager` (x1), `the_unsolved_problem` (x1), and `invisible_performance_management` (Track 2, x1). **PHASE 8 SUPERSEDES, verified against live pipeline:** `the_unsolved_problem`'s Phase 8 mitigation (commit 4c1a5de) has since eliminated its false-rank-1 footprint entirely — `EXP-CO-02` now also goes to `invisible_performance_management`. `resolution_family` "Intervention + Roadmap" — the cluster's own larger group. |

---

## Open investigative questions (separate from the per-state tracker)

| Question | Plain-language description | Status | Notes |
|---|---|---|---|
| Cross-cluster asymmetry mechanism | When two whole clusters compete on the same dimension (like rank-2 vs. rank-3), what actually decides which one wins? We ruled out "the sharper/more concentrated vector wins" — it's backwards in the one case checked (`the_uninitiated`, the less concentrated one, beats rank-3, the sharper one). | **WORKING THEORY FOUND, Program Phase 2 — not yet proven, to be tested against real interventions starting Phase 3** | **Mechanism (traced mathematically, confirmed against real `dimension_summary` data, not assumed):** the weighted-cosine formula self-normalizes using each state's OWN salience weights on both the numerator and that state's own denominator term — concentrating weight on one axis inflates that state's own denominator too, so sharpness only pays off if the real session signal is purely aligned with that one axis. Checked 15+ real profiles across all 3 known cases: **real session signal is never purely single-axis** — e.g. `the_founders_grip`'s own 3 dedicated profiles show attitude (0.47-0.52) as the strongest real component, not authority (0.33-0.36), despite being purpose-written as an Authority state; `the_broken_compass`'s strongest real signal is Authority (0.398), not its own dominant Attitude (0.278). States with a higher floor or an elevated secondary axis capture that off-axis "impurity" for real credit; pure single-axis states get nothing extra there and pay the bigger self-normalization penalty for their sharpness — this is why "sharper wins" is backwards. **FALSIFIED ALTERNATIVE:** pure vector magnitude — `the_founders_grip`'s total weighted magnitude is actually LARGER than `the_uninitiated`'s (0.974 vs. 0.785), yet it loses. Not about size in isolation, about shape-match to real impure signal. **SYNTHESIS OF THREE DISTINCT IMPLEMENTATIONS, not one interchangeable lever** — confirmed as three different real shapes, not one mechanism wearing three names: (1) uniform floor raise across all non-dominant axes, no single elevated secondary (`the_uninitiated`'s shape); (2) targeted secondary-axis raise, both vector and salience elevated on one specific real-text-grounded axis (`culture_drift`'s Authority secondary); (3) dual-primary-axis, two substantially-weighted axes rather than one maxed-out axis (`the_unexamined_algorithm`'s Authority+Aptitude). Future fixes must pick whichever lever the real text actually supports for the specific state in question — not assume any one of the three is a general-purpose fix. **STRATEGIC IMPLICATION for Phases 3-6, not acted on yet:** fixing a losing cluster's own territory-loss likely means adding a real, text-grounded secondary-axis/floor elevation to the LOSING side (the precedent that's actually worked twice, `culture_drift` and `the_unexamined_algorithm`), not sharpening the winning side further — sharpening would very likely backfire by this same mechanism. No fix proposed or written this phase — theory only, confirmed relevant to `the_uninitiated`, `the_unexamined_algorithm`, and `culture_drift`, the same three cases already on record. |
| Rank-5 / rank-6 decomposition check | The 8-cluster characterization pass gave rank-5 and rank-6 a stakes/narrative read, but never ran the tie-vs-genuine decomposition check (the one that found `the_uninitiated`, `the_unexamined_algorithm`, `the_second_close`, and `culture_drift` were all more complicated than they looked). | **DONE for rank-6** | Rank-6 fully decomposed this session (tools/_scdwcs_decomposition_rank6.py): all 3 states (`the_second_close`, `silosolation`, `the_arbitrary_standard`) confirmed SAME-CLUSTER DIFF, real cross-cluster footprint small and fully accounted for (2 external wins + 3 tie-artifacts, nothing hiding). Rank-5's equivalent question was effectively answered by its pilot's own baseline verification (confirmed 175/175 exact tie pre-pilot) rather than a separate decomposition pass — see the rank-5 tracker rows. |
| `the_overloaded_manager` anomaly | Steals only from attitude-dominant targets despite being aptitude-dominant itself, with an authority secondary — doesn't match its own vector on any axis. One case checked at the session-vector level, still unexplained. | OPEN | n=4 is too small to trust any theory yet. Needs more data (more theft profiles as other clusters get worked, or a deeper trace of the actual centroid-displaced vector) before this gets its own theory. |
| `narrative_lock`/`the_dormant_talent` fragility | `narrative_lock` loses 2 of its 3 own dedicated profiles to `the_dormant_talent` (rank-7, Aptitude) by a razor-thin margin (0.951888 vs. 0.951290, a gap of 0.0006) — found during Phase 1's rank-4 confirmation (tools/_scdwcs_decomposition_rank4.py). Not a Track 2 pattern (single rival, not a broad attractor) and not the cross-cluster asymmetry shape either (single rival, not a whole competing cluster). | OPEN, flagged not scoped | Any future touch to `the_dormant_talent`'s `dimensional_vector` or `SALIENCE_PROFILES` could tip this margin either way — worth checking against this specific pair before any future `the_dormant_talent` work, even work unrelated to rank-4. Not scoped further this phase. |
| **Systematic, BIDIRECTIONAL `dimension_summary` confound across both Authority- and Attitude-flavored profiles** | `generate_answers()` appears to produce a real accumulated session signal that cross-wires Authority and Attitude regardless of a profile's actual target-state text content — confirmed running BOTH directions, not a one-way quirk. **Direction 1 (Phase 5):** Authority-flavored profiles show Attitude-dominant real signal — `compression_crisis`, rank-2's 5 mis-clustering candidates, `the_founders_grip` (Phase 2), `sequential_decision_blindness`/`disparate_impact_architecture` (Phase 4) — 9 states. **Direction 2 (Phase 6, confirmed across ALL 11 rank-1 states checked):** Attitude-flavored profiles show Authority-dominant real signal — `the_diversity_ceiling`, `the_burned_credibility`, `invisible_burnout`, `the_basement_standard`, `the_inside_track`, `groundhog_day`, `the_wrong_reward`, `the_broken_compass`, `human_displacement_anxiety`, `motivational_architecture_failure`, `cultural_overtime` — 11 more states. **Confirmed footprint now 20 states across 4 program phases (2, 4, 5, 6) and 3 different clusters (rank-2, rank-3, rank-1) plus 2 individually-checked states — this is a genuine two-way generator artifact, not a one-directional quirk specific to Authority-flavored content.** | OPEN, needs its own investigation, independent of any single cluster | **Practical resolution holds and is now confirmed bidirectionally, not full root-cause:** the raw `dimension_summary` ranking is NOT a reliable mis-clustering signal in either direction — confirmed by running the actual mechanical weight-raise falsification test on 5 total candidates across both directions (`the_policy_lag`, `the_pay_fog` — Direction 1; `invisible_burnout`, `groundhog_day`, `the_inside_track` — Direction 2). 4 of 5 showed real, monotonic own-score DECREASES matching a genuinely-wrong-axis candidate; `the_inside_track` showed a genuine but impractically weak POSITIVE trend (see its own row) — a third category, still not a false positive, confirming the test's sensitivity in both directions. Combined with `paper_shield`/`the_unsolved_problem`'s clean positive results, the mechanical test has now been validated across every outcome shape (clean positive, clean negative, weak positive) — it reliably discriminates real signal from the confound regardless of which direction the confound is pulling. **The open question is still why the confound exists at all, and now specifically why it runs both ways** — likely something structural in how `generate_answers()` constructs answers for both Authority- and Attitude-flavored profile types, worth its own investigation independent of any cluster's remediation work, and relevant to any future mis-clustering scoping anywhere in the taxonomy (rank-1 through rank-11 and beyond). Not investigated further this phase. |
| `invisible_burnout` compounding fragility | `invisible_burnout` is one of the 4 standing known pre-existing failures (2/3), caused by something unrelated to this session's work. Full ripple audit (tools/_scdwcs_ripple_audit.py, Program Phase 6) found it now also carries TWO new competitors on top of that standing issue: `paper_shield` wins 1 of its 3 own profiles (`ATT-IB-01`), `the_unsolved_problem` wins the other 2 (`ATT-IB-02/03`) — between them, all 3 of `invisible_burnout`'s own profiles are now won by one or the other. | OPEN, flagged not scoped, watch before any future touch to either state | Confirmed the aggregate pass count is still 2/3, unchanged from before either re-clustering existed — not a new regression today. But this is a compounding-pressure situation, same category as the `narrative_lock`/`the_dormant_talent` razor-thin-margin fragility: any future magnitude increase to either `paper_shield` or `the_unsolved_problem` (e.g. if Phase 8's Track 2 work or a future session touches either) should check this specific state before proceeding, since it's now sitting at the intersection of two independent new competitors rather than one. Not scoped further this phase. **PHASE 8 SUPERSEDES, verified against live pipeline:** Phase 8 DID subsequently touch both states (commit 4c1a5de) — checked as anticipated. `ATT-IB-01` now goes to `invisible_performance_management` (was `paper_shield`), `ATT-IB-02/03` now go to `built_to_fail` (was `the_unsolved_problem`) — still all 3 own profiles lost, still to a Track 2-style attractor, just under different specific names. Counted pass status confirmed unaffected (still 2/3, the same standing known failure, verified via the standard regression suite throughout Phase 8's work). |
| Unique-vector states losing profiles to this session's re-clustered states | Low-priority completeness note from the full ripple audit: `the_suppression_filter` (no tracked cluster) now loses 2 of its own 3 profiles to `paper_shield`; `distributed_culture_fragmentation` (no tracked cluster) loses 1 of its 3 to `the_unsolved_problem`. | LOW PRIORITY, logged for completeness, no action needed | Both states' own pass status confirmed unaffected (3/3 each). Neither belongs to any of this session's worked ranks (1 through 11) — flagged here rather than on any cluster's own row since neither has one. **PHASE 8 SUPERSEDES, verified against live pipeline:** both states' Phase 8 mitigations (commit 4c1a5de) have since eliminated their false-rank-1 footprints entirely — `the_suppression_filter`'s 2 profiles now go to `invisible_performance_management`, `distributed_culture_fragmentation`'s 1 profile now goes to `built_to_fail`. Own pass status still unaffected (3/3 each). |
| **Ripple audit summary — this session's 3 vector/salience changes, full accounting** | `paper_shield` (9 new wins), `the_unsolved_problem` (19 new wins), `planning_authority_gap` (4 new wins + 1 pre-existing strengthened) — **32 total newly-surfaced winner instances**, spanning at least 6 different clusters/unique-vector states: rank-1, rank-2, rank-4, rank-9 (DECLINED pair), rank-10 (already-differentiated pair), rank-11, plus 2 unique-vector states with no tracked cluster. | CLOSED — completeness confirmed as of the commits named; **PHASE 8 SUPERSEDES the paper_shield/the_unsolved_problem portions specifically (28 of the 32 instances) — all 28 re-verified against the live pipeline and confirmed to now resolve to a different true winner (mostly `invisible_performance_management`/`built_to_fail`, two to `the_unformed_leader`) following Phase 8's mitigation (commit 4c1a5de), which eliminated both states' false-rank-1 footprints entirely (9->0, 19->0). Each individual affected row (culture_drift, the_unreported_hazard, the_unlocked_door, the_untouchable, the_inner_circle, dueling_narratives, transition_paralysis, the_wrong_reward, human_displacement_anxiety, motivational_architecture_failure, cultural_overtime, invisible_burnout, plus the unique-vector-states row) carries its own specific correction, not reproduced here. `planning_authority_gap`'s 4+1 portion is UNAFFECTED (its own configuration was never touched by Phase 8) and remains accurate as originally recorded. Zero counted pass/fail status changed anywhere by this correction — confirmed via the standard regression suite throughout.** | Empirically confirms the "recurring pattern, not isolated caveat" finding from Program Phase 6 Stage A. Full exhaustive method used (every one of 175 profiles checked both pre- and post-change for all 3 states, not spot-checked), same rigor as rank-7's original pilot verification standard. Zero NEW counted pass/fail failures found anywhere among the 19 distinct target states touched — the two states showing non-3/3 status (`invisible_burnout`, `the_untouchable`) were already at that status before any of this session's re-clustering work existed. No disposition changes made anywhere as a result of this audit — see the individual completeness notes on `the_unlocked_door`, `the_unreported_hazard`, `decision_blindness`, `the_untouchable`, and `the_inner_circle`'s own rows. |
| **Phase 8 closeout — Track 2 dominance pool, taxonomy census** | Why do `built_to_fail`/`invisible_performance_management` overpower so much of the suite, and is it just those two? | **CENSUS COMPLETE, root cause understood; 2 of 6 active states mitigated, 4 of 6 remain OPEN, unresolved** | Full 58-state sweep found 37 states share the exact standard "primary-only" salience template + liability-skewed vector — a taxonomy-wide default authoring pattern (Session 17's tier convention), not a handful of anomalies. Of those 37, only 6 actually manifest false-rank-1 dominance: `invisible_performance_management` (44), `built_to_fail` (41), `the_unsolved_problem` (19), `the_uninitiated` (17), `paper_shield` (9), `the_second_close` (5) — the other 31 sit at exactly zero despite sharing the identical shape, confirming the template alone doesn't predict dominance; something axis-specific in `generate_answers()` (plausibly related to the already-documented Authority/Attitude confound, apparently extending to Aptitude) determines which specific states become attractors. **2 of the 6 (`paper_shield`, `the_unsolved_problem`) were this-session re-clustering side effects, confirmed via `git show` reconstruction to have measured exactly 0/175 before their re-clustering — not pre-existing, genuinely manufactured — and both are now SHIPPED with a mitigation closing them to 0/175** (see their own rows). **The other 4 (`built_to_fail`, `invisible_performance_management`, `the_uninitiated`, `the_second_close`) are pre-existing and remain unresolved.** Whack-a-mole confirmed empirically: reducing `built_to_fail`'s own salience alone, `invisible_performance_management`'s alone, or both jointly, caps out at 1 of the 19 currently-masked states gaining any real payoff — suppressing the whole pool simultaneously just promotes a 4th state (`paper_shield`, before its own mitigation) to fill the vacancy. Confirmed NOT fixable via any single-state or combined salience lever tested — the real fix requires taxonomy-wide vector/template re-authoring, a distinct and much larger future undertaking, not scoped further this session. **Methodological gap flagged for all future re-vectoring work:** full-175-profile false-rank-1 screening should become standard practice alongside regression/collision checks — neither `paper_shield` nor `the_unsolved_problem`'s original re-clustering searches could have caught their manufactured footprint, because this measurement didn't exist until Phase 8. **CORRECTION, Phase 9 (2026-08-21) — see the dedicated Phase 9 rows directly below, all six census numbers now individually re-verified:** all six of the numbers recorded in this row's own sentence above came from ONE single execution of `tools/_scdwcs_full_hierarchy_measurement.py` (the census script itself confirms via its own docstring cross-reference — `tools/_scdwcs_phase8_track2_diagnostic.py` — that this measurement ran post-re-clustering, pre-mitigation), so all six shared identical staleness exposure, not just the one caught first. **Re-verified individually, corrected results: `invisible_performance_management` 44 -> real 59 (drift, see its own Phase 9 row); `built_to_fail` 41 -> real 52 (drift, see its own Phase 9 row); `the_uninitiated` 17 -> CONFIRMED UNCHANGED at 17 (see its own Phase 9 row); `the_second_close` 5 -> CONFIRMED UNCHANGED at 5 (see its own Phase 9 row).** `paper_shield` (9) and `the_unsolved_problem` (19) are the two states the mitigation itself targeted — already independently re-verified at 0/175 post-mitigation on their own rows, not re-touched here. **Pattern confirmed:** only the two states sharing `paper_shield`/`the_unsolved_problem`'s own Aptitude axis (`invisible_performance_management`, `built_to_fail`) actually drifted; the two Authority/Alliance-axis states (`the_uninitiated`, `the_second_close`) did not — consistent with the drift being a real axis-specific ripple effect, not a general staleness across all census figures regardless of dimension. |
| **Phase 9 diagnostic pass (2026-08-21) — `invisible_performance_management` full live-data pull + false-rank-1 correction** | Ahead of any Track 2 vector reshape, pull every piece of live source data on the taxonomy's single largest dominance problem and re-verify it has no re-clustering option, per Pete's clinical-completion directive (fix every affected state individually, not just close the investigation). | **DIAGNOSTIC COMPLETE, no fix proposed or numbers tested this phase** — full raw output in `tools/_scdwcs_ipm_diagnostic.py` (untracked, scratch-only, re-runnable) | **Live data confirmed (direct read, engine/data/states.py + engine/data/salience.py):** `dimensional_vector` — aptitude_liability=0.45/aptitude_asset=0.15 (dominant, 3:1 skew, standard Track 2 shape) — but `authority_liability` sits at 0.25, not the 0.15 floor every other non-dominant axis carries (alliance 0.15/0.15, attitude 0.10/0.15) — an already-authored, currently-INERT secondary: `SALIENCE_PROFILES` weights all three non-dominant axes identically at flat 0.4/0.4, so this vector-level Authority elevation earns zero real credit today. `resolution_family` confirmed "Development + Roadmap". `descriptive_prose` verbatim: "A manager's read on an underperforming employee is accurate but was never written down, so it carries no evidentiary weight when a decision needs defending. This isn't concealment. It's an absence of documentation that turns a sound judgment into an exposed one." **Cluster-mate check (exact `dimensional_vector` match, direct code trace against all 58 states): ZERO matches** — confirmed genuinely unique vector, no re-clustering target exists, matching the prior finding exactly. (Salience-TEMPLATE-only matches, not vector matches, noted for completeness: `built_to_fail`, `the_undefined_role`, `the_unsolved_problem` share the identical flat aptitude-primary salience shape — same Phase 8 census pool of 37, not a re-clustering option.) **Own dedicated profiles reconfirmed directly (EXP-IPM-01/02/03): all 3 lose outright to `built_to_fail` specifically, every time (own_rank=2, e.g. EXP-IPM-03 own_score=0.931138 vs. built_to_fail's 0.957713)** — zero true rank-1 ever, confirmed, and the identical single attractor each time, not a mix. **FALSE-RANK-1 COUNT CORRECTED, real discrepancy, not a script error:** this session's own opening framing ("44/175, 25.1%, down from an original 59/175") does not match a fresh measurement against the current live pipeline. Cross-validated two independent ways — a purpose-built full sweep and the harness's own `build_confusion_matrix()` output via `python tools/calibration_runner.py` (grepped for `-> invisible_performance_management`, hand-summed to 59) — both return 59/175 (33.7%), byte-identical to the "original" pre-investigation figure, not 44. **Root cause traced:** `tools/_scdwcs_phase8_track2_diagnostic.py`'s own docstring confirms its "44" was measured "post paper_shield/the_unsolved_problem re-clustering" — but before their own later same-session salience mitigation (paper_shield Attitude 0.4->2.0, the_unsolved_problem aptitude_asset 0.15->0.20, commit 4c1a5de), both confirmed live in the current codebase by direct read this phase. Working explanation, not independently proven further: the two re-clustered states' move onto Aptitude temporarily pulled some Aptitude-flavored profiles away from `invisible_performance_management` (59->44); their own later mitigation pulled their false-rank-1 footprints back to 0/175 each, and the profiles they'd been borrowing reverted to `invisible_performance_management`, restoring it to 59. Every OTHER row this mitigation touched carries a "PHASE 8 SUPERSEDES, verified against live pipeline" correction reflecting exactly this ripple pattern — `invisible_performance_management`'s own row was the one never re-verified, because the mitigation's target was the other two states, not this one. **Corrected here — any future reshape must be scoped against 59/175, not 44.** **New data, not previously recorded:** false-rank-1 footprint broken down by the true target's own `primary_dimension`, across all 32 distinct competing states / 59 profiles — Attitude 25/59 (42%), Authority 24/59 (41%), Alliance 6/59 (10%), Aptitude 4/59 (7%). Confirms the existing row's "pure vector-strength dominance, doesn't concentrate on its own authored Aptitude axis" characterization with real numbers for the first time — the footprint is genuinely near-evenly split across the taxonomy's two largest dimensions, not concentrated in one direction. Full per-profile detail (all 59 test_ids, true target, true target's own rank/score, `invisible_performance_management`'s winning score) and all 32 true-target states' own live `dimensional_vector`/`SALIENCE_PROFILES`/`descriptive_prose` recorded in the diagnostic script's output — not reproduced in this table, this row is the summary. **No vector or salience reshape proposed, searched, or numbers tested this phase — data-gathering only, per this session's explicit scope.** |
| **Phase 9 continuation (2026-08-21) — `built_to_fail`'s own live data + false-rank-1 dimensional breakdown; `invisible_performance_management`'s top-10 competitor field-by-field overlap check** | Ahead of any real reshape proposal for either Track 2 state (comparative groundwork before this goes near Gemini): pull `built_to_fail`'s own vector/salience/prose directly for the first time (previously only referenced via `the_paper_tiger`'s rank-8 tie-break); confirm whether `built_to_fail`'s 9 profiles reverted from paper_shield/the_unsolved_problem/invisible_performance_management's own dedicated tests concentrate on the shared Aptitude axis as the mechanism theory predicts; and check field-by-field whether `invisible_performance_management`'s own non-floor vector fields actually collide with real textual content in its highest-volume competitors, or win on pure magnitude with no field-level basis. | **DIAGNOSTIC COMPLETE, no fix proposed or numbers tested** — full raw output in `tools/_scdwcs_phase9_comparative.py` (untracked, scratch-only, re-runnable) | **`built_to_fail` own live data, pulled for the first time (direct read):** `dimensional_vector` — aptitude_liability=0.60/aptitude_asset=0.10 (6:1 skew, sharper than `invisible_performance_management`'s 3:1), authority/alliance/attitude all flat at the 0.1 floor (not `invisible_performance_management`'s 0.15 — a different floor convention, no secondary anywhere). `SALIENCE_PROFILES`: standard flat aptitude-primary template (2.5/2.5 dominant, 0.4/0.4 elsewhere), identical shape to `invisible_performance_management`'s. `resolution_family`: "Roadmap + Intervention". `descriptive_prose` verbatim: "The role's scope exceeds what any reasonable allocation of resources could support, and each person who holds it is told to make it work rather than given what making it work would require. The organization treats each departure as an individual hiring failure rather than a structural one. The next person inherits the same impossible math." Cluster-mate check (exact `dimensional_vector` match): exactly 1 — `the_paper_tiger`, already known and already SHIPPED (own-salience tie-break, see that row) — no new re-clustering option surfaced. **`built_to_fail`'s 52-profile dimensional breakdown, and the reverted-profile check, CONFIRMED:** full breakdown — Aptitude 21/52 (40.4%), Attitude 16/52 (30.8%), Authority 9/52 (17.3%), Alliance 6/52 (11.5%). The 9 reverted profiles (AUT-PS-01/02/03, AUT-UP-01/02/03, EXP-IPM-01/02/03) are **100% Aptitude** — every one, confirmed directly, exactly matching the mechanism theory from the prior Phase 9 row (they're `built_to_fail`'s own Aptitude-axis cluster-mates by construction). The remaining 43 profiles are NOT Aptitude-concentrated — Attitude is actually their largest share (16/43, 37.2%), ahead of Aptitude (12/43, 27.9%), then Authority (9/43, 20.9%) and Alliance (6/43, 14.0%) — confirming `built_to_fail`'s broader, non-reverted dominance is genuinely cross-dimensional, same "doesn't concentrate on its own authored axis" shape already found for `invisible_performance_management`, not an Aptitude-specific effect. **`invisible_performance_management` top-10 competitor pull, field-by-field overlap against its own non-floor fields (aptitude_liability=0.45, authority_liability=0.25, attitude_liability=0.10 [suppressed, below its own 0.15 floor]):** top 5 by stolen-profile count in each dimension — Attitude: `the_untouchable`(3), `the_wrong_reward`(3), `culture_drift`(3), `the_basement_standard`(2), `what_nobody_says`(2); Authority: `pay_exposure`(3), `the_pay_fog`(3), `the_unexamined_algorithm`(3), `compression_crisis`(3), `planning_authority_gap`(3). **CORRECTION TO THE RAW SCRIPT OUTPUT, caught and fixed before writing this up:** the script's own overlap flag used a single hardcoded 0.15 floor threshold, which mislabels any state authored on the 0.1-floor convention (`the_untouchable`, `the_unexamined_algorithm`) as "elevated above floor" when some of those fields are actually just that state's own genuine floor — corrected by hand below, not taken from the raw flag. **Real findings, per-state floor-corrected:** `the_untouchable`, `the_wrong_reward`, `the_basement_standard`, `what_nobody_says` show **ZERO genuine field-level overlap** with `invisible_performance_management` — each is a clean single-axis Attitude state (or, for `what_nobody_says`, Alliance-secondary) with every other field at its own true floor; their loss to `invisible_performance_management` is pure magnitude/self-normalization dominance, not vector collision, consistent with the taxonomy's existing cross-cluster asymmetry theory. `culture_drift` is the one exact numeric collision found: its `authority_liability` (0.25, a real elevated secondary per its own already-documented text) matches `invisible_performance_management`'s `authority_liability` (0.25) exactly — same field, same magnitude, different textual basis on each side (culture_drift: unstated cultural/reward drift; `invisible_performance_management`: undocumented managerial judgment) — flagged as a genuine collision point, not claimed as a shared mechanism. In the Authority group, `pay_exposure`/`the_pay_fog`/`compression_crisis`/`planning_authority_gap` all share real (non-floor) signal with `invisible_performance_management` on `authority_liability` only — but each of their own values (0.45, 0.45, 0.45, 0.35) sits well above `invisible_performance_management`'s 0.25, and each carries real Authority salience (2.5/2.5 or 0.5/0.5) against `invisible_performance_management`'s flat 0.4/0.4 — on pure per-field weighting these four states should out-compete it on their own axis, so `invisible_performance_management`'s actual win over them (confirmed in the false-rank-1 sweep) is not explained by this field-level overlap and is more evidence for the broader self-normalization mechanism, not a vector-collision story. **`the_unexamined_algorithm` is the single most textually-grounded double overlap found in this whole pull:** its own `aptitude_liability` (0.35, a real elevated secondary above its own 0.1 floor — not floor, correctly flagged even by the buggy script) sits genuinely close to `invisible_performance_management`'s dominant 0.45, AND its `authority_liability` (0.5, its own dominant) is real alongside `invisible_performance_management`'s 0.25 secondary — both fields collide with real signal on both sides, and its own text ("no governance layer reviewing... nobody owns auditing its outputs") independently supports a genuine Authority+Aptitude dual-axis read, already flagged elsewhere in this tracker's cross-cluster theory discussion. Full per-state `dimensional_vector`/`SALIENCE_PROFILES`/`descriptive_prose` for all 10 competitor states recorded in the diagnostic script's output — not reproduced in this table. **No vector or salience reshape proposed, searched, or numbers tested this phase.** **CLOSED, `invisible_performance_management` sharpening pilot (2026-08-21), this session:** the `authority_liability` "collision" flagged above for `culture_drift`, `pay_exposure`, `the_pay_fog`, and `compression_crisis` is explicitly CLOSED as NOT a real collision for all four — a shared numeric value with no shared textual mechanism against `invisible_performance_management`'s actual prose (a documentation/evidentiary gap for a human judgment call, not values-drift, compensation-market tracking, or headcount/capacity compression). Do not touch these four states' fields on this basis. (`the_unexamined_algorithm` is unaffected by this closure — its own double-field overlap has independent, genuine textual grounding on both sides, addressed separately in the sharpening pilot's Part 2, not this closure.) |
| **Phase 9 continuation-2 (2026-08-21) — `invisible_performance_management` full 32-state overlap classification + sandbox concentration/skew mechanism test** | Extend the prior top-10 overlap sample to all 32 true-target competing states (does field collision explain most of the 59, or a minority?), and directly test whether vector concentration/skew alone — separate from field content — measurably moves the false-rank-1 count, using the same corrected floor-detection methodology throughout. | **DIAGNOSTIC COMPLETE, no fix proposed.** Classification: `tools/_scdwcs_phase9_full_classification.py` (untracked, scratch-only, re-runnable). Skew sweep: session scratchpad only (`_scdwcs_ipm_skew_sweep.py`), deliberately NOT written to `tools/` and NOT committed — a mechanism test, not a candidate fix, per this session's explicit instruction | **TASK 1 — full 32-state classification (extends the top-10 sample):** of the 59 profiles / 32 states, **16 states (25/59 profiles, 42.4%) show ZERO genuine field overlap** — pure magnitude/self-normalization dominance, no shared elevated field with `invisible_performance_management`'s own aptitude_liability/authority_liability. **16 states (34/59 profiles, 57.6%) show genuine field collision** — 15 single-field (31 profiles, 14 of 15 collide on authority_liability only, 1 on aptitude_liability only) and 1 double-field (`the_unexamined_algorithm`, 3 profiles, collides on both — already flagged in the prior top-10 pass as the most textually-grounded case). **So field-level collision is NOT a minority explanation — it accounts for a genuine majority (57.6%) of the footprint by profile count**, though "collision" here means shared non-floor magnitude on a field, not necessarily a shared textual mechanism (the `pay_exposure`/`the_pay_fog`/`compression_crisis`/`planning_authority_gap` group all collide on authority_liability while each individually out-weighing `invisible_performance_management` on that exact field on paper — their loss to it anyway argues the collision itself isn't the deciding factor for that subgroup, consistent with the top-10 pass's finding). Full per-state detail (all 32 states, collision fields, profile counts) in the classification script's output, not reproduced here. **TASK 2 — concentration/skew mechanism test, sandbox only:** held `invisible_performance_management`'s total liability budget fixed at 0.90 (the taxonomy's established single-axis convention — confirmed via `built_to_fail`'s own 0.60+0.10x3=0.90 and every standard single-axis state's 0.45+0.15x3=0.90), built a PURE single-axis test vector (dominant aptitude_liability=X, the other three liability fields flat at Y, X+3Y=0.90, no secondary content — deliberately removing the real authority_liability secondary and attitude_liability suppression to isolate concentration alone), and swept the dominant:floor skew ratio from 1:1 (fully uniform) to 20:1 (far past `built_to_fail`'s own 6:1) against the real 175-profile pipeline. **RESULT: the relationship is NON-MONOTONIC, not the simple line the 2-point (`invisible_performance_management` vs. `built_to_fail`) comparison suggested.** False-rank-1 by ratio: 1:1->0, 1.5:1->9, 2:1->28, 2.5:1->38 (**peak**), 3:1->36, 3.5:1->31, 4:1->29, 4.5:1->24, 5:1->19, 5.5:1->10, 6:1->7, 7:1->5, 8:1->3, 10:1->3, 15:1->0, 20:1->0. Dominance is near-zero at BOTH extremes (a fully flat vector has no directional signal to dominate with; an extremely sharp vector pays too large a self-normalization penalty) and peaks at moderate concentration (ratio ~2.5:1, just below `invisible_performance_management`'s real current 3:1). **This corrects the naive extrapolation from n=2:** `invisible_performance_management` (3:1) and `built_to_fail` (6:1) both sit on the DECLINING side of this curve, past the peak — so "built_to_fail is sharper and dominates less" is real but doesn't mean sharper always dominates less; a state slightly LESS concentrated than `invisible_performance_management` (ratio ~2.5:1) would be predicted to dominate MORE, not less, by this same isolated lever. **Quantified concentration-alone vs. content split:** at `invisible_performance_management`'s own real ratio (3:1), the pure single-axis test gives 36/175 — its REAL vector (with the authority_liability secondary and attitude_liability suppression left in) gives 59/175, confirmed this session. **So concentration alone, at its own current skew, accounts for roughly 36 of the 59 (61%); the remaining ~23 (39%) is attributable to field content specifically (the secondary/suppression), not concentration.** Sharpening further (toward or past `built_to_fail`'s 6:1, or all the way to 15:1+) COULD close most or all of the pure-concentration portion (36->7->0 across that range) — concentration alone is a real, measurable lever, not a dead end — but was never tested in combination with the real secondary content, so the content-attributable ~23 remains an open, separate question this sweep didn't answer. **Own-profile finding, same shape as before, now confirmed across the FULL tested range:** `invisible_performance_management` NEVER wins its own 3 dedicated profiles at any tested ratio (true_rank1=0/3 every time, 1:1 through 20:1) — own rank improves from 9-10 at full uniformity down to a floor of rank 2 from ratio 2.5:1 onward, but `built_to_fail` holds rank 1 there regardless of how concentrated `invisible_performance_management`'s vector becomes. **Confirms concentration alone cannot fix the own-profile loss — that is a separate problem from the broad false-rank-1 footprint.** Regression stayed at the 171/175 baseline across nearly the whole range (a real dip to 170/175 at ratios 1.5-2.5, exactly the peak-dominance zone, not the low zone — worth noting, not chased further) with zero new failures beyond the 4 standing pre-existing ones and zero of those 4 resolved at any tested point. **No numbers proposed as a fix — this is mechanism characterization only, and the script/output are explicitly not committed per this session's instruction.** |
| **`decision_paralysis`/`the_lost_map` tie-break reversal inside a previously CLOSED Program Phase 5 entry (2026-08-21)** | A prior "closed" finding on `decision_paralysis`'s own row claimed it and `the_lost_map` "genuinely WIN their own 3 dedicated profiles outright" via the rank-2 internal tie. Found incidentally this session while breaking down `the_uninitiated`'s false-rank-1 detail — no longer true. | **OPEN, flagged for a dedicated future investigation, not blocking Track 2** | Found incidentally during this session's `the_uninitiated` breakdown, not the target of a deliberate search. Root cause NOT chased down — timing of the shift, and which specific change (if any, this session or earlier) caused the tie-break-winner order to move, is unknown. Does not move any counted pass/fail number (the lenient prominence criterion passes a tie either way, so the standing 171/175 regression figure never reflected this either before or after). Full correction detail already recorded on `decision_paralysis`'s own row — not duplicated here, this row is the pointer. Not blocking Track 2 remediation work (built_to_fail/invisible_performance_management reshape scoping can proceed independent of this). |
| **`invisible_performance_management` own-profile loss to `built_to_fail` — distinct from the broad-theft problem (2026-08-21)** | Why does `invisible_performance_management` never win even its own 3 dedicated profiles (EXP-IPM-01/02/03)? | **OPEN, needs its own dedicated investigation. Not addressed by the sharpening pilot, not blocking its commit.** | Confirmed flat across every concentration ratio tested — both the sandbox mechanism test (ratio 1:1 through 20:1) and this session's sharpening pilot (Part 1, ratio 3.5:1 through 50:1) — `invisible_performance_management` loses all 3 of its own dedicated profiles to `built_to_fail` specifically, every single time, at every tested magnitude. **Not a magnitude question** — sharpening `invisible_performance_management`'s own vector arbitrarily far never closes this gap in isolation (own_rank floors at 2, `built_to_fail` holds rank 1 regardless). Needs its own dedicated investigation into why `built_to_fail` specifically (not some other state) wins `invisible_performance_management`'s own best-case test profiles, independent of the broad 59-profile false-rank-1 theft problem this session's pilot targets. |
| **`invisible_performance_management` concentration-sharpening pilot, Part 1 — NOT RECOMMENDED for pilot-scale pursuit (2026-08-21)** | Does sharpening `invisible_performance_management`'s aptitude concentration (holding the 0.90 liability budget invariant, pure single-axis, ratios 3.5:1 through 50:1) close its 59/175 false-rank-1 footprint at an acceptable cost? | **TESTED, real result recorded. NOT RECOMMENDED for pursuit at pilot scale — exceeds this pilot's scope, needs a Gemini-gated structural review.** | Full raw output: `tools/_scdwcs_ipm_sharpen_pilot_part1.py` + `_part1b_top3check.py` (untracked, scratch-only). **Real result: sharpening DOES close the headline number** — false-rank-1 falls from 59/175 to 0/175 by ratio 12:1-20:1 (7/175 already at ratio 6:1, matching `built_to_fail`'s own concentration). Score ripples for every OTHER state confirmed exactly zero at every tested ratio (per-state scoring is properly independent, verified not assumed) and the standing 171/175 regression never moved. **But this comes at a real, previously-invisible cost:** `invisible_performance_management`'s own score shifts on 118-128 of the 175 profiles (not just the ones it steals), and this cascades into **130-183 top-3 rank crossings for OTHER states across the taxonomy** at the ratios that actually close the false-rank-1 number (ratio 6:1 -> 130; ratio 12:1-20:1, the range that reaches 0/175 -> 178-183). These are output-relevant, not cosmetic — real sessions surface up to 2 secondary states (weight >= 0.20) alongside the primary rank-1 state, so a rank-2/3 crossing for an uninvolved state on an uninvolved profile is a real behavior change the false-rank-1 metric alone never captured. **Flagged explicitly: this is taxonomy-wide ranking behavior change, not single-state magnitude tuning** — a single-field concentration adjustment on one state legitimately moving rank position on ~130-180 other state/profile pairs across the whole suite is a materially different kind of change than the rank-5 through rank-9 same-cluster differentiation pilots this session's Tier 1 mechanism was modeled on, none of which touched anything beyond their own 1-3 state cluster. **This is now hard, measured evidence for the standing "Track 2 needs a real taxonomy-wide re-authoring project, not a same-cluster salience pilot" finding** (Phase 8 closeout row) — not a new conclusion, but the first time that finding has real ripple numbers behind it rather than the whack-a-mole own-profile-payoff argument alone. Not recommended for pursuit at pilot scale as currently scoped. Would need a Gemini-gated structural review before any further work, per the standing Tier 1/Tier 3 distinction (CLAUDE.md, Workflow Governance) — this pilot's dry-run/scratch mechanism was the right starting point for finding this out, but the finding itself has outgrown that mechanism. |
| **Combined candidates (Part 1 + Part 2 together) — tested, non-additive, WORSE than Part 1 alone (2026-08-21)** | Does combining Part 1's sharpening with Part 2's `the_unexamined_algorithm` differentiation produce the sum of their individual effects, or interference? | **TESTED. Real interference confirmed — both combined candidates underperform Part 1 alone at the same ratio.** | Full raw output: `tools/_scdwcs_ipm_combined_part3.py` (untracked, scratch-only). Two combined candidates tested (Part 1's sharpening at ratio=6.0 and ratio=15.0, each + Part 2 Candidate C's light-touch differentiation on both states' colliding field): **ratio=6.0 combined -> `invisible_performance_management` false-rank-1 = 61/175 — WORSE than doing nothing (baseline 59/175)**, and worse than Part 1 alone at that ratio (7/175). **ratio=15.0 combined -> 33/175 — far worse than Part 1 alone at that ratio (0/175).** Score ripples for other states confirmed zero in both combined candidates; full regression held at 171/175, same 4 known failures, unchanged. **Root cause not fully traced, but the direction is clear: stapling two independently-optimized single-lever candidates together does not produce their independent effects' sum — real geometric interference between the two vector changes.** A genuine joint search (varying both fields together from scratch, not combining two separately-chosen candidates) would be needed to know whether a combined fix is possible at all — not attempted this session, out of scope for this pilot. **One incidental finding, explicitly NOT carried forward as standalone evidence:** at the ratio=15.0 combined candidate, `invisible_performance_management` won one of its own three dedicated profiles for the first time all session (EXP-IPM-03, rank 1, beating `built_to_fail`) — flagged as CONFOUNDED, not standalone signal, since it appeared only inside an already-rejected combined candidate (61/175 and 33/175 both underperform Part 1 alone) and has not been isolated or reproduced independently. Does not move the separate own-profile-loss investigation (see that row, directly above) forward — that investigation should not treat this as a lead without independent reproduction. |
| **`invisible_performance_management` / `the_unexamined_algorithm` differentiation, Part 2 — ripple-audited, READY FOR PETE'S COMMIT REVIEW (2026-08-21)** | Following Part 1's finding that ripple cost was invisible until directly measured, the same full audit (score invariance + top-3 rank-crossing count against every other state) was run against Part 2's own candidates, which hadn't had it yet. | **RIPPLE-AUDITED. READY FOR PETE'S COMMIT REVIEW** — real, non-zero ripple disclosed below, not claimed clean; substantially smaller in kind than Part 1's systemic footprint, not just in count. | Full raw output: `tools/_scdwcs_ipm_uea_part2_ripple_check.py` (untracked, scratch-only). All 3 original Part 2 candidates ripple-checked: **Candidate A** (`invisible_performance_management` authority_liability 0.25->0.20 only): false-rank-1 59->43, top-3 ripples = **27**. **Candidate B** (`the_unexamined_algorithm` aptitude_liability 0.35->0.25 only): false-rank-1 9->2, top-3 ripples = **21**. **Candidate C** (light touch both, 0.20/0.30 — the candidate carried into Part 3's combined test): `invisible_performance_management` false-rank-1 59->43, `the_unexamined_algorithm` false-rank-1 9->5, top-3 ripples = **36**. Score ripples confirmed exactly zero for all 3 candidates (per-state scoring independence holds). Regression held at 171/175 in all 3, same 4 known failures, zero new. **Comparison against Part 1's systemic footprint:** Candidate C's 36 top-3 ripples is roughly 4-5x smaller than Part 1's 130-183 — a real, qualitative difference in kind, not just a smaller instance of the same problem: Part 1's ripples were sprawling and taxonomy-wide (`invisible_performance_management`'s own score shifted on 118-128/175 profiles, most having nothing to do with its target competitors); Part 2's 36 ripples are concentrated among a modest, enumerable set of profile/state pairs, mostly `built_to_fail`/`the_uninitiated`/`the_undefined_role` trading rank-2/3 positions on specific already-related profiles. **No direct historical baseline exists for "typical successful-pilot ripple count"** — this exact top-3 rank-crossing check was invented this session (Part 1), so earlier rank-5 through rank-9 pilots were never measured against it; this comparison is against Part 1's own number, the only same-methodology reference available, not an established house standard. Full ripple detail (all profile/state pairs) recorded in the audit script's output for Pete's direct review before any commit decision — not reproduced in this table. **No code, vector, or salience changes made — still dry-run, pending Pete's review of this result.** **STATUS UPDATE (2026-08-21):** superseded by the closing section directly below — Candidate C's flagged-state overlap detail (built_to_fail/the_uninitiated leakage) surfaced after this row was written, changing the disposition from "ready for commit review" to "held pending sequencing." See the closing section row for the current status. |
| **CLOSING SECTION — `invisible_performance_management`/`the_unexamined_algorithm` pass (2026-08-21), documentation close-out only** | Consolidated record of this multi-session pass on the taxonomy's largest Track 2 dominance problem, before moving on. No code, vector, or salience changes this row — Candidate C held, not shipped. | **CLOSED as a documentation pass. Candidate C HELD pending sequencing — not a technical defect, a values/sequencing call for Pete.** | **(1) Full A/B/C ripple comparison, same methodology throughout (score-invariance check + top-3 rank-crossing count against every other state):**<br>— Candidate A (`invisible_performance_management` authority_liability 0.25->0.20 only): 27 total top-3 ripples. `built_to_fail` 10 (37%, 9 gains/1 loss). `the_uninitiated` 1 (4%, 1 gain/0 losses).<br>— Candidate B (`the_unexamined_algorithm` aptitude_liability 0.35->0.25 only): 21 total. `built_to_fail` 2 (10%, 2 gains/0 losses). `the_uninitiated` 8 (38%, 8 gains/0 losses).<br>— Candidate C (light touch both, 0.20/0.30): 36 total. `built_to_fail` 12 (33%, 11 gains/1 loss). `the_uninitiated` 4 (11%, 4 gains/0 losses).<br>`the_second_close`, `paper_shield`, `the_unsolved_problem`: zero occurrences in any of the three candidates, throughout. Score ripples confirmed exactly zero in all three (per-state scoring independence holds); regression held 171/175 in all three, same 4 known failures, zero new. **(2) Key mechanism finding, direct profile-level cross-check, not inferred:** `built_to_fail`'s leakage is IPM-driven, not combination-specific — Candidate A alone (37%) sits close to Candidate C's share (33%), while Candidate B alone barely touches it (10%). Touching `invisible_performance_management`'s `authority_liability` field at all appears to structurally feed `built_to_fail`'s separate dominance problem, regardless of which differentiation method is used or whether `the_unexamined_algorithm` is touched at all. `the_uninitiated`'s leakage is UEA-driven and is PARTIALLY SUPPRESSED, not added to, by the combined candidate — 38% isolated (Candidate B) vs. 11% combined (Candidate C). Direct cross-check of the actual profile/state pairs confirmed the mechanism, not just the aggregate percentage: Candidate B alone produces 9 `the_uninitiated`-related ripples across AUT-PL-01/02/03 (3 each — `the_uninitiated`, `leadership_continuity_risk`, `decision_paralysis` all shift); none of those 9 appear in Candidate C at all — `invisible_performance_management`'s own change, added on top, reverses `the_unexamined_algorithm`'s effect on those 3 specific profiles. Everywhere else, Candidate B's ripples (AUT-HC-03, ALL-DB-01, AUT-LC-01/03) carry through into Candidate C unchanged. **(3) Status: Candidate C HELD, not committed, pending sequencing decision.** The `the_unexamined_algorithm` differentiation itself remains a real, well-grounded, small fix — textually justified on both sides, zero score contamination, no new regression, and a ripple footprint (36, 44% flagged) an order of magnitude more contained than Part 1's systemic 130-183. This is not a technical defect blocking commit. **Recommendation on record:** address `built_to_fail`'s own open-profile-loss investigation (see that row, this table) first, then re-evaluate Candidate C against whatever baseline that work leaves behind — a `built_to_fail` fix would very likely reshape this exact ripple picture, given `built_to_fail` accounts for the largest single share of Candidate C's flagged leakage (33%) and that leakage is confirmed IPM-driven, not an artifact of the specific UEA differentiation being evaluated. Re-running Candidate C's ripple check after any `built_to_fail` work lands, rather than deciding now against a baseline likely to move, is the recommended sequencing — Pete's call, not decided here. **(4) Full Track 2 state as of this pass:** `invisible_performance_management` — dual-problem confirmed (61% concentration-attributable / 39% field-collision-attributable), no clean single fix found; concentration-sharpening ruled out at pilot scale (Part 1, hard ripple evidence for the taxonomy-wide re-authoring finding); the `the_unexamined_algorithm` collision fix is ready-but-held per this closing section. `built_to_fail` — own live data pulled for the first time this session (vector/salience/prose), own-profile-loss-to-nothing n/a (it's the attractor, not the victim, on its own profiles), but its own-dimension breakdown done and its role as the structural driver of Candidate C's leakage now confirmed; no fix proposed. `the_uninitiated` — confirmed cross-cluster asymmetry against rank-3 (100% Authority-dimension footprint), unchanged this pass, standing characterization holds. `the_second_close` — confirmed single-dimension (100% Alliance), unchanged this pass, standing characterization holds. **All four still require Pete's clinical judgment before any commit — consistent with the standing finding that Track 2 needs real taxonomic authoring work, not further magnitude searches.** No commits made. This is a documentation close-out only. |
| **`built_to_fail` own-profile-loss investigation, CLOSED — uniformly ARTIFACT (magnitude/concentration), not field-level collision (2026-08-22)** | Does `built_to_fail` win `invisible_performance_management`'s (IPM) own 3 dedicated profiles (EXP-IPM-01/02/03) via genuine field-level collision (a real shared textual mechanism, the standard already applied to IPM/`the_unexamined_algorithm`) or a magnitude/concentration artifact (`built_to_fail`'s vector shape structurally dominates regardless of content)? Gates Candidate C's re-evaluation per the closing section above's recommendation. | **DIAGNOSED — ARTIFACT, uniformly across all 3 profiles, not a mix. No fix proposed, searched, or tested — diagnostic only, per explicit scope.** | Full raw output: `tools/_scdwcs_btf_ipm_ownprofile_investigation.py` (untracked, scratch-only, re-runnable). Zero engine, vector, or salience files touched — confirmed via `git status` before and after. **Method: per-field decomposition of rank_states()'s own weighted-cosine formula, hand-reproduced and cross-validated bit-exact against the real engine's own score/rank output on all 3 profiles x both states (6/6 exact matches to 1e-9) before trusting the breakdown.** Both states' real live vector/salience pulled fresh this pass (unchanged from Phase 9's prior pull): IPM — aptitude_liability=0.45/aptitude_asset=0.15 (dominant, 3:1), authority_liability=0.25 (elevated secondary), alliance/attitude at floor (0.15/0.15/0.10); `built_to_fail` — aptitude_liability=0.60/aptitude_asset=0.10 (6:1, sharper), everything else flat at the 0.1 floor, no secondary anywhere. Both share the identical flat aptitude-primary `SALIENCE_PROFILES` template (2.5/2.5 dominant, 0.4/0.4 elsewhere) — same weights on both sides for every field. **Headline finding: on all 3 profiles, `aptitude_liability` alone accounts for MORE than the entire final numerator gap** (EXP-IPM-01: 0.734982 of a 0.691700 total, 106%; EXP-IPM-02: 0.828732 of 0.738700, 112%; EXP-IPM-03: 0.866232 of 0.755400, 115%) — **every other field, summed together, actually favors IPM, not `built_to_fail`**, and is swamped by the single shared axis. This is the same axis both states are taxonomically authored around (both are aptitude_liability-primary, part of Phase 8's 37-state standard-template pool) — `built_to_fail` simply carries a higher raw value on that shared primary axis (0.60 vs 0.45), not a distinct field colliding with independent textual support the way IPM/`the_unexamined_algorithm`'s double-field overlap does. **Correction to a prior characterization, caught in this pass:** IPM's `authority_liability` secondary (0.25) is NOT actually salience-"inert" as the earlier live-data-pull row above states — the per-field breakdown shows it genuinely reduces `built_to_fail`'s advantage on every one of the 3 profiles (a real, negative-gap, pro-IPM contribution each time: -0.019, -0.019, -0.054), it is just an order of magnitude too small to counteract the aptitude gap. "Inert" was an overstatement of "not given elevated salience weight above the shared 0.4 floor" — the vector-level elevation does earn real, measurable credit at that shared weight, just not enough. **Mechanical falsification test, in-memory only, real vector restored after every check:** swept IPM's own `aptitude_liability` concentration from its real 3:1 through 200:1 (33x past `built_to_fail`'s own real 6:1) against all 3 profiles individually. **IPM's own score never overtakes `built_to_fail`'s fixed score at ANY tested ratio, on ANY of the 3 profiles** — own score is non-monotonic (peaks near ratio 6-10, then declines at extreme sharpness, e.g. EXP-IPM-02: 0.9263 at 3:1 -> 0.9358 at 6:1 -> 0.9278 at 200:1), consistent with the project's already-established concentration/self-normalization-penalty curve (Phase 9 sandbox sweep, this tracker). **This rules out "IPM is simply insufficiently concentrated relative to `built_to_fail`" as the mechanism** — if magnitude parity alone explained the loss, IPM should overtake once its own ratio exceeds `built_to_fail`'s 6:1, and it does not, even 33x past that point. `built_to_fail`'s specific finite vector shape sits at a locally favorable point in the weighted-cosine geometry against these exact profiles that no purely-sharper single-axis IPM vector can close — a subtler magnitude/geometry effect than a flat "sharper always wins" rule, but a magnitude/geometry effect nonetheless, not content-driven. **Independent textual cross-check, consistent with the quantitative finding:** all 3 EXP-IPM descriptions (a documentation-gap-in-an-accurate-managerial-judgment story, twice; a weak/minimal-signal profile once) contain zero language resembling `built_to_fail`'s actual mechanism (role-scope/understaffing/"impossible math" — see its own descriptive_prose, Phase 9 row above) — no shared narrative basis exists on either side, reinforcing the artifact classification rather than contradicting it. **Cross-validated two independent ways, per standing discipline:** (1) the per-field decomposition formula matched the real engine's own `rank()` output bit-exact on all 3 profiles x both states before any conclusion was drawn from it; (2) the 30-point mechanical falsification sweep (10 ratios x 3 profiles) independently and consistently reproduces the same ceiling effect. **Recommendation on record, not decided here:** per the closing section's own sequencing note above, a `built_to_fail` fix "would very likely reshape" Candidate C's ripple picture — this row's finding sharpens that into a specific expectation: since the mechanism here is confirmed magnitude/geometry (the same class of problem Phase 8 and the concentration-sharpening pilot already showed is NOT tractable via a same-cluster salience pilot), any future `built_to_fail` remediation is likely a Track-2-style broad reshape question, not a quick pilot -- consistent with `built_to_fail` already being named as such in Phase 8's closeout. No numbers proposed, no fix scoped -- Pete's call on sequencing and whether to pursue. |
| **Candidate C sequencing decision, Pete's call (2026-08-22)** | Following the built_to_fail own-profile-loss investigation directly above: does Candidate C (the `the_unexamined_algorithm` differentiation) get re-evaluated against the current baseline now, or held further? | **DECIDED — HELD INDEFINITELY, folded into the taxonomy-wide re-authoring bucket. No code, vector, or salience changes.** | Pete's decision, given the investigation's finding that `built_to_fail`'s advantage over IPM is a magnitude/geometry effect, not a collision, and not resolvable at the single-field level: Candidate C stays HELD rather than being re-evaluated against the current baseline now. Folded into the standing taxonomy-wide vector/template re-authoring bucket alongside `built_to_fail`'s own problem -- both now read as the same underlying class of issue (magnitude/geometry, not same-cluster differentiation or field collision), not two separately-solvable items. Re-evaluate only if future taxonomy-wide work changes the baseline, not on any set schedule. |
| **Candidate C re-evaluation against current baseline (2026-08-23)** | Pete's decision: revisit Candidate C against the built_to_fail investigation's finding rather than leave it held further. Does the flagged 33% built_to_fail-driven ripple leakage change, stay the same, or turn out independent of that finding? | **RE-EVALUATED — headline numbers UNCHANGED, byte-identical to the original audit. Recommendation: technically ready as originally designed; the hold's original rationale (wait for a built_to_fail fix, re-check after) no longer applies, since no such fix exists as a near-term possibility — this is now a direct ship/hold judgment call for Pete, not a technical question awaiting more data.** | **Confirmed before running anything:** zero engine/vector/salience files have changed since `a0ccbdb` (`git diff --stat a0ccbdb HEAD -- engine/` empty) — the built_to_fail investigation was diagnosis-only, so "current" and "prior" baseline are mechanically identical; the raw ripple numbers could not have changed regardless of what was run. **Candidate C's exact values, pulled directly from this table's own CLOSING SECTION row, not memory:** `invisible_performance_management` `authority_liability` 0.25 -> 0.20; `the_unexamined_algorithm` `aptitude_liability` 0.35 -> 0.30 ("light touch both"). **Cross-validated two independent ways:** (1) re-ran the original `tools/_scdwcs_ipm_uea_part2_ripple_check.py` unmodified; (2) wrote a fresh, independent reimplementation (`tools/_scdwcs_candidateC_reevaluation_20260823.py`, untracked, re-runnable) from scratch, same methodology, to catch any bug the original script might share with itself. **Both agree exactly, and both match the historical record exactly:** false-rank-1 IPM 59->43, UEA 9->5; 36 top-3-crossing ripples; regression 171/175, same 4 pre-existing failures (`ATT-IB-02`, `ATT-IE-02`, `ATT-LD-02`, `ATT-UT-02`), zero new. **Full per-state ripple breakdown, computed fresh this pass (the closing section recorded the top two shares but not the complete table):** `built_to_fail` 12/36 (33.3%, 11 gains/1 loss) -- exact match to the recorded 33%; `the_overloaded_manager` 6/36 (16.7%, 6 gains/0 losses) and `the_undefined_role` 6/36 (16.7%, 6 gains/0 losses) -- both new to this pass's explicit table, not previously broken out by name; `the_uninitiated` 4/36 (11.1%, 4 gains/0 losses) -- exact match to the recorded 11%; `leadership_continuity_risk`, `decision_paralysis`, `the_dormant_talent` at 2/36 (5.6%) each; `the_paper_tiger` 1/36 (2.8%, its only loss in the set) and `planning_authority_gap` 1/36 (2.8%). **Direct answer to the task's central question: the leakage estimate does not change under re-evaluation -- it is mechanically identical, confirmed empirically, not just assumed from the git-diff check.** **What DOES change is the interpretation, not the number.** The closing section's original recommendation ("address built_to_fail's own open-profile-loss investigation first, then re-evaluate Candidate C against whatever baseline that work leaves behind -- a built_to_fail fix would very likely reshape this exact ripple picture") assumed a built_to_fail fix was a realistic near-term event to wait for. The investigation that concluded at `a0ccbdb` closes that possibility: built_to_fail's advantage over IPM is confirmed magnitude/geometry, not a same-cluster or single-field collision, and per that investigation's own recommendation, any real fix is "a Track-2-style broad reshape question, not a quick pilot" -- the same large, undated taxonomy-wide vector/template re-authoring project already on record elsewhere in this tracker, not a scoped near-term fix. **This reinforces rather than weakens the substance of the concern, even though it removes the reason to keep waiting:** built_to_fail's 11 gains (of its 12 ripples) are it moving INTO better rank positions on profiles it doesn't actually belong to -- given built_to_fail is independently confirmed as an already-over-dominant broad attractor, these gains inflate a known problem, not a neutral side effect, and the closing section's own mechanism finding (touching IPM's `authority_liability` field specifically feeds built_to_fail's dominance, regardless of method) is corroborated rather than contradicted by the newer investigation. **Bottom line, not decided here:** Candidate C's own values are textually well-grounded on both sides (IPM/UEA), produce zero score contamination, zero new regression, and a ripple footprint that is real but bounded and now fully characterized, not a mystery. The condition the "held pending sequencing" status was waiting on has resolved to "not coming in any near-term, scoped form" -- so continuing to hold it as "pending" is no longer an accurate characterization of the situation. Whether that means ship, revise, or hold anyway on the substance of the 33%/11% leakage picture is Pete's call, not decided or acted on here. **Confirmed via `git status`: zero engine, vector, or salience files modified by this pass.** |

---

## How to use this

Pete fills in the "Pete's call" column whenever he has a real instinct
about what makes a state distinct — no pressure to fill every row, no
deadline. Claude.ai/CC read this file at the start of any SCD-WCS work to
pick up wherever Pete's left notes, and update the Status column as work
happens. This file is the single source of truth for this investigation's
discretionary judgment calls — don't duplicate this content into MOB
Section 13a beyond a pointer reference.
