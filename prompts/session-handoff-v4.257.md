# PRV3 Session Handoff — MOB v4.257

Direct extract/reformatting of this session's Section 16 entry in `tools/_mob.txt` (2026-08-28, one entry: the SCD-WCS dominant-states remediation program's full five-stage arc and its closure). Not independently authored — if this ever appears to contradict Section 16, Section 16 is authoritative.

**Note on the "carried forward" section below:** several items handed to this closeout as open/carried-forward were checked against `tools/_mob.txt` directly before being written here, per this project's standing verification discipline — three turned out to already be closed (MemPalace/Mem0 retirement, `/book/toc` fuller vision, the "What NOT to Document" article), not open as initially framed. Corrected below rather than transcribed as given; flagged explicitly so the correction itself isn't lost.

## What this session covered

**SCD-WCS dominant-states remediation program CLOSED — full disposition on all four Phase-8-confirmed dominant states, `evaluate_candidate()` extended to joint multi-state candidates (commit `4be4971`).** Retires the program's own single largest carried-forward item (Section 13a Decision Register's "SCD-WCS / primary-state ranking investigation" row; Section 13b Priority Queue item 3), open since Phase 8's 2026-08-21 close. Five-stage arc, working the four states Phase 8 confirmed "not fixable via salience alone" one at a time.

**`the_second_close` (Stage 1): CLOSED, already fixed.** The premise driving this stage (5/175 false-rank-1, no dedicated investigation) was stale before any work started — a fresh live sweep found a prior session's own full re-authoring program (`dimensional_vector` budget-expanded 0.90→1.05, aptitude secondary added, shipped commit `f88a7c2`, 2026-08-25) had already cut this to 1/175. Candidate search on the residual (`ALL-FR-01`, a genuine win against `the_fracture`) found no viable single-state fix — the driving field (aptitude secondary) is inert in the safe range and actively regressive below it; the alliance axis redirects the win to a third state (`the_suppression_filter`) rather than restoring the true target. Flagged, not chased — single profile, low volume.

**`the_uninitiated` / rank-3 cluster (Stages 2, 3, 5, 5b): DIAGNOSED, mechanically tested, PARKED — Pete's decision, same disposition as `built_to_fail`.** Mechanism found and mathematically confirmed, not just correlated: rank-3 (8 members, all byte-identical `dimensional_vector`+`SALIENCE_PROFILES`, `invisible_performance_management` newly joined this exact-match group via its own 2026-08-25 re-authoring) never wins a single one of its 24 own dedicated profiles, to anyone, at any measured point — 0/24, confirmed by direct `_weighted_cosine_similarity()` term-by-term breakdown: the shared 0.60 peak concentration pays a quadratic self-normalization penalty (2.5×0.6²=0.900, 94.8% of its own squared magnitude) that a flatter same-axis rival's linear numerator gain outweighs on any profile carrying real off-axis signal — confirmed against `the_uninitiated` and three other genuinely flatter rivals (`planning_authority_gap`, `the_paper_tiger`, `the_undefined_role`), all winning by the identical mechanism.

**IPM cross-checked and confirmed NOT part of this mechanism, twice, independently.** First (Stage 3, profile-loss analysis): IPM's own peak (0.60 aptitude, on `built_to_fail`'s axis) is EQUAL to rank-3's peak, not lower — the concentration-asymmetry mechanism above cannot apply; IPM's loss to `built_to_fail` is a same-magnitude, different-axis numerator race, confirmed narrow (2 of 8 rank-3 members affected, not broad) and already fully explained by `built_to_fail`'s own pre-existing, already-halted Track 2 dominance (2026-08-25 decision, four negative results). Second (Stage 5, mechanical test): a disposable 8-state flattening proof-of-concept produced 8 new false-rank-1 wins and 13 new top-3 ripples — re-running the identical candidate with IPM excluded (Stage 5b) produced the EXACT SAME 8 flips and 13 ripples, just credited to a different (still-flattened) state by registration order — direct proof the collateral is a property of the flattened vector shape shared by the group, not specific to IPM's own calibration content, and a correction to this session's own initial Stage 5 read.

**Mechanical fix attempted and confirmed non-viable, two-stage proof-of-concept.** Flattening rank-3's shared peak from 0.60 toward the lowest known-winning rival's concentration (0.35) closes the gap to an exact tie at 0.30 with zero collateral — but sweeping further down (0.28 through 0.20, 7-state subset, IPM excluded) never achieves a genuine own-profile win (0/21 throughout, one fragile tie-order credit at 0.30/0.28 that doesn't survive the sweep) while collateral grows monotonically worse (8→31 flips, 13→40 ripples). The "starts helping" and "starts hurting" boundaries coincide — no daylight to exploit. Confirmed clean on the one collateral direction this stage specifically checked: IPM's own 3 profiles never flip toward any of the 7, at any tested value. **Pete's decision: park the rank-3 cluster fix alongside the already-parked `built_to_fail`** — both confirmed dominant, both mechanism-diagnosed, neither has a viable fix at the mechanical level; real per-state text-grounded authoring across 8 states would be needed even to make further mechanical progress, not scoped or started.

**`invisible_performance_management`: CLOSED, fully explained, no new work.** Confirmed an instance of the already-halted `built_to_fail` problem, not a third independent mechanism — already covered by the 2026-08-25 halt decision, nothing further needed.

**`built_to_fail`: unchanged this session, its own precedent confirmed to generalize.** The IPM cross-check is the first confirmed second instance of `built_to_fail`'s specific "equal-concentration, cross-axis numerator win" pattern beyond its own already-documented footprint.

**Tooling built and shipped along the way (Stage 4, commit `4be4971`).** `evaluate_candidate()` extended to accept a joint list of `(state_id, new_vector, new_salience)` tuples, applied atomically and scored simultaneously — required since SCD-WCS's per-state self-normalization makes sequential single-state evaluation of a joint candidate mathematically invalid, and directly supersedes last session's own "scope boundary" note that this was single-state-only by design. **Verification:** zero behavioral drift confirmed on the existing single-state path (byte-identical re-run of a prior sweep and a prior direct CLI call); a 2-state no-op plus two error-path checks (unknown state_id in a joint list → clean `KeyError`, zero mutation leaked; `new_vector` alongside a list → `ValueError` as documented) confirmed the new path's correctness before any real candidate was run against it; full 11-script Python engine suite re-run clean, 775/775, identical to session-start baseline. Gemini-reviewed; all 7 of its factual claims independently verified against live source first (6 PASS, 1 FAIL — its proposed `0.90` liability-sum invariant was verified FALSE against live source, an unenforced convention already violated by 5 shipped states, and dropped, not built).

**Net effect, closing the framing this whole program has carried since Phase 8 (2026-08-21):** the "four confirmed dominant states, not fixable via salience alone" list is now fully disposed of — two resolved/explained (`the_second_close` already fixed by a prior session, `invisible_performance_management` fully explained as `built_to_fail`'s problem, not its own), two remain genuinely dominant with confirmed mechanisms but no viable fix (`built_to_fail`, the rank-3 cluster) — both now explicitly PARKED by Pete's decision, not open questions.

**No `engine/data/states.py` or `engine/data/salience.py` changes anywhere in this session's SCD-WCS work** except the already-existing, prior-session `f88a7c2` — every candidate tested this session was disposable, evaluated via `evaluate_candidate()`'s guaranteed rollback, confirmed via `git diff --stat` empty after every run. Only two real code/docs commits this session: `4be4971` (the `evaluate_candidate()` extension) and `0284ecf` (the MOB/tracker closure documentation).

## Status at close

The SCD-WCS dominant-states program has a real, documented endpoint for the first time — not an indefinitely-open item. `evaluate_candidate()` now supports genuine joint multi-state candidate evaluation, a real tooling capability that will outlive this specific closure if the rank-3/`built_to_fail` parking is ever revisited. Full derivation, every sweep table, and per-profile detail lives in `prompts/scd-wcs-remediation-tracker.md`'s Stage 1 through Stage 5(b) sections.

## Open — carried forward, not started or not resolved this session

1. **SCD-WCS's own remaining two dominant states (`built_to_fail`, rank-3 cluster)** — mechanism-diagnosed, mechanically confirmed unfixable at the mechanical level this session, PARKED per Pete's decision (see Closed/Parked below — listed here only to note that "parked" is not "solved"; real per-state text-grounded authoring would be the next lever if this is ever reopened, not scoped or started).
2. **Deployment Protection / no Preview environment / custom domain** — unchanged this session. The site is publicly reachable today at `prv-3.vercel.app`, unbranded, with no Preview environment as a safety net. Custom domain wiring (Porkbun) still pending — sequencing recommendation on record is to wire it after a real transaction path exists, not before. Not verified fresh this session — carried forward from `tools/_mob.txt` Section 13a.
3. **Real Transaction Path** — Phase 1 (Dropbox Sign e-signature engage flow) shipped in an earlier session (commit `5b9586f`) but carries manual steps not fully closed out; Phase 2 (the webhook) has not been started at all. Untouched this session — carried forward unverified, not re-checked.
4. **Signature-chip Neutral-theme contrast gap** — the Warm/Dark `--slate` migration shipped (commit `838ac43`), but its Neutral-theme contrast gap was explicitly logged as its own still-open item in `tools/_mob.txt` Section 13 at the time, not folded into that ship. Not independently re-verified or re-traced this session — if picked up, start by reading Section 13's body text directly (the exact row wasn't re-derived here) plus `web/components/SignatureCard.tsx` and `web/app/globals.css`.
5. Pre-existing carried-forward items unchanged this session (per the prior handoff, not re-verified): `session-store.test.ts`'s 6 stale sequence-length failures (last checked 2026-08-18); OSHA actual-average-penalty backfill (14 states, not urgent); `STATE_CAUSATION_OVERRIDES` per-state authoring (undated); ADA/FMLA/OSHA headcount gating (new logic, not started); `tools/diagnostic_fast_forward.py` rework-or-retire decision; the `organization_size` string|number type collapse, dated ~2026-09-04.

## Corrections made while preparing this handoff — three items were NOT actually open

Checked against `tools/_mob.txt` directly before writing anything into this durable document, per standing discipline. All three were already closed, contrary to the framing this closeout was initially given:

- **Mem0/MemPalace retirement — CLOSED, not blocked.** The graduation test PASSED (2026-08-26, real `mem0.vector_stores.qdrant` read-back, not a raw-storage bypass) and the full retirement (steps 2 and 3, Mem0 rewiring, `mempalace_*` removed from `.mcp.json`) shipped the same session, commit `fab74f2`. Section 13a's "MemPalace retirement" row status reads CLOSED. Nothing to attach or resume here.
- **`/book/toc` fuller vision — CLOSED, all four phases shipped and live-verified.** Phase 4 (verification) was confirmed live via Pete's own interactive click-through against the running dev server, not just server-rendered HTML. Not "awaiting Pete's elaboration."
- **"What NOT to Document" (LIB-052) — already fully drafted, cited, and committed**, commit `d1f69d8`, MOB v4.31 (July 2026, Session 59) — months before this session. If there's a *new*, separate follow-on idea Pete has in mind for a related piece, that's a fresh item to scope, not a continuation of LIB-052 itself, which is finished.

## Closed this session

SCD-WCS dominant-states remediation program (full disposition, all four Phase-8 states, park decision on the two unfixed ones). `evaluate_candidate()` joint multi-state extension (built, verified, shipped).

## Parked — do not resurface unless Pete reopens

Attorney review of the Engagement Agreement / OneDigital covenant question (long-standing, parked indefinitely, Pete's explicit call). LinkedIn campaign go-live (blocked entirely on attorney review, not separately tracked). Real Transaction Path Phase 1's remaining manual steps and Phase 2 entirely (see Open, item 3). **New this session:** the rank-3 cluster fix and `built_to_fail`'s own dominance — both confirmed, both mechanism-diagnosed, both parked per Pete's explicit decision, not open questions requiring a future pursue/don't-pursue call.

## Standing / dated items

- **Quarterly Step-Back** — biweekly cadence. Last step-back: August 23, 2026. Next due: **on or near September 6, 2026** — not yet due.
- `organization_size` type collapse: dated ~2026-09-04, untouched, see Open item 5.

## Files to attach next session

- **Always:** `tools/_mob.txt` (current version, v4.257).
- **If the SCD-WCS thread somehow reopens despite being closed:** `tools/scdwcs_validator.py` (now supports joint multi-state candidates), `tools/scdwcs_candidate_search.py`, `prompts/scd-wcs-remediation-tracker.md` (full Stage 1-5b record), `engine/data/states.py` / `engine/data/salience.py`.
- **If resuming the Neutral-theme signature-chip contrast gap:** `web/components/SignatureCard.tsx`, `web/app/globals.css` — and read `tools/_mob.txt` Section 13's body text directly first, since the exact open-item wording wasn't re-derived this session.
- **If resuming the real transaction path (Phase 1 cleanup or Phase 2):** `web/app/api/engage/initiate/route.ts`, whatever Phase 1 completion-status doc exists from the shipping session (not verified this session — locate fresh).
- **Mem0/MemPalace retirement, `/book/toc` fuller vision, "What NOT to Document":** nothing to attach — all three are closed, see Corrections section above.

## Mem0 diary status

Diary write completed this closeout via `tools/prv3_diary.py` (subprocess call, terminal Claude Code environment) — confirmed successful, topic `scd-wcs-dominant-states-program-closed`.
